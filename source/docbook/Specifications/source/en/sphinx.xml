<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="http://docbook.org/xml/5.1/rng/docbook.rng" schematypens="http://relaxng.org/ns/structure/1.0"?>
<?xml-model href="http://docbook.org/xml/5.1/sch/docbook.sch" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>

<!-- The XSL transform inserts these values as Hugo frontmatter. -->
<!-- Additionally, a "date" timestamp is inserted by the stylesheet. -->

<?title "" ?> <!-- Required ... copy section title here [https://gohugo.io/content-management/front-matter/#title] -->
<?linkTitle "Sphinx cryptographic packet format" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#linktitle] -->
<?description "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#description] -->
<?author "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#params] -->
<?url "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#url] -->
<?draft "false" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#draft] --> 
<?slug "sphinx" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#slug] -->
<?layout "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#layout] -->
<?type "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#type] -->
<?weight "55" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#weight] -->
<?version "" ?> <!-- Optional [?] -->

<!DOCTYPE section [
    <!ENTITY % shared-content SYSTEM "/home/dwrob/git/website/source/docbook/shared-content.ent">
        %shared-content;
    <!ENTITY % references SYSTEM "/home/dwrob/git/website/source/docbook/references.ent">
        %references;
    <!ENTITY % terms SYSTEM "/home/dwrob/git/website/source/docbook/terms.ent">
        %terms;    
    <!ELEMENT xi:include (xi:fallback?) > 
    <!ATTLIST xi:include 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" 
    href CDATA #REQUIRED 
    parse (xml|text) "xml" 
    xpointer CDATA #IMPLIED 
    encoding CDATA #IMPLIED 
    accept CDATA #IMPLIED 
    accept-charset CDATA #IMPLIED 
    accept-language CDATA #IMPLIED > 
    <!ELEMENT xi:fallback ANY > 
    <!ATTLIST xi:fallback 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" > 
    <!ENTITY % local.preface.class "| xi:include"> 
    <!ENTITY % local.part.class "| xi:include"> 
    <!ENTITY % local.chapter.class "| xi:include"> 
    <!ENTITY % local.divcomponent.mix "| xi:include"> 
    <!ENTITY % local.para.char.mix "| xi:include"> 
    <!ENTITY % local.info.class "| xi:include"> 
    <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink"
    version="5.1" xml:id="sphinx">
    <info>
        <title xml:id="sphinx.title">Sphinx cryptographic packet format specification</title>
        <authorgroup>
            <author>
                <personname>
                    <firstname>Yawning</firstname>
                    <surname>Angel</surname>
                </personname>
            </author>
            <author>
                <personname>
                    <firstname>George</firstname>
                    <surname>Danezis</surname>
                </personname>
            </author>
            <author>
                <personname>
                    <firstname>Claudia</firstname>
                    <surname>Diaz</surname>
                </personname>
            </author>
            <author>
                <personname>
                    <firstname>Ania</firstname>
                    <surname>Piotrowska</surname>
                </personname>
            </author>
            <author>
                <personname>
                    <firstname>David</firstname>
                    <surname>Stainton</surname>
                </personname>
            </author>
        </authorgroup>
        <abstract>
            <para>This document defines the Sphinx cryptographic packet format for decryption mix
                networks, and provides a parameterization based around generic cryptographic
                primitives types. This document does not introduce any new crypto, but is meant to
                serve as an implementation guide. </para>
        </abstract>
    </info>




    <section xml:id="terminology">
        <title xml:id="terminology.title">Terminology</title>
        <para>The following terms are used in this specification.</para>
        <variablelist>  
        &groupelement;
        &groupgenerator;
        &header;
        &message;
        &payload;
        &packet;       
        </variablelist>
    </section>
    <section xml:id="conventions-used-in-this-document">
        <title xml:id="conventions-used-in-this-document.title">Conventions used in this
            document</title>
        <para>The key words <quote>MUST</quote>, <quote>MUST NOT</quote>, <quote>REQUIRED</quote>,
                <quote>SHALL</quote>, <quote>SHALL NOT</quote>, <quote>SHOULD</quote>, <quote>SHOULD
                NOT</quote>, <quote>RECOMMENDED</quote>, <quote>MAY</quote>, and
                <quote>OPTIONAL</quote> in this document are to be interpreted as described in <link
                linkend="RFC2119">RFC2119</link>. </para>
        <para>The <emphasis>C</emphasis> style Presentation Language, as described in §4 of <link
                linkend="RFC5246">RFC5246</link>, is used to represent data structures, except for
            cryptographic attributes, which are specified as opaque byte vectors. </para>
        <itemizedlist spacing="compact">
            <listitem>
                <para>
                    <code>x | y</code> denotes the concatenation of x and y. </para>
            </listitem>
            <listitem>
                <para>
                    <code>x ^ y</code> denotes the bitwise XOR of x and y. </para>
            </listitem>
            <listitem>
                <para>
                    <code>byte</code> an 8-bit octet. </para>
            </listitem>
            <listitem>
                <para>
                    <code>x[a:b]</code> denotes the sub-vector of x where a/b denote the
                    start/end byte indexes (inclusive-exclusive); a/b may be omitted to signify the
                    start/end of the vector x respectively. </para>
            </listitem>
            <listitem>
                <para>
                    <code>x[y]</code> denotes the <emphasis>y</emphasis>th element of list x. </para>
            </listitem>
            <listitem>
                <para>
                    <code>x.len</code> denotes the length of list x. </para>
            </listitem>
            <listitem>
                <para>
                    <code>ZEROBYTES(N)</code> denotes N bytes of 0x00. </para>
            </listitem>
            <listitem>
                <para>
                    <code>RNG(N)</code> denotes N bytes of cryptographic random data. </para>
            </listitem>
            <listitem>
                <para>
                    <code>LEN(N)</code> denotes the length in bytes of N. </para>
            </listitem>
            <listitem>
                <para>
                    <code>CONSTANT_TIME_CMP(x, y)</code> denotes a constant time comparison
                    between the byte vectors x and y, returning true iff x and y are equal. </para>
            </listitem>
        </itemizedlist>
    </section>

    <section xml:id="sphinx_introduction">
        <title xml:id="sphinx_introduction.title">Introduction</title>
        <para>The Sphinx cryptographic packet format is a compact and provably secure design
            introduced by George Danezis and Ian Goldberg <link linkend="SPHINX09">SPHINX09</link>.
            It supports a full set of security features: indistinguishable replies, hiding the path
            length and relay position, detection of tagging attacks and replay attacks, as well as
            providing unlinkability for each leg of the packet’s journey over the network. </para>
    </section>
    <section xml:id="cryptographic-primitives">
        <title>Cryptographic primitives</title>
        <para>This specification uses the following cryptographic primitives as the foundational
            building blocks for Sphinx: </para>
        <itemizedlist>
            <listitem>
                <para>
                    <code>H(M)</code> – A cryptographic hash function which takes an octet array M
                    to produce a digest consisting of a <code>HASH_LENGTH</code> byte octet array.
                        <code>H(M)</code> MUST be pre-image and collision resistant. </para>
            </listitem>
            <listitem>
                <para>
                    <code>MAC(K, M)</code> – A cryptographic message authentication code function
                    which takes a <code>M_KEY_LENGTH</code> byte octet array key <code>K</code> and
                    arbitrary length octet array message <code>M</code> to produce an authentication
                    tag consisting of a <code>MAC_LENGTH</code> byte octet array. </para>
            </listitem>
            <listitem>
                <para>
                    <code>KDF(SALT, IKM)</code> – A key derivation function which takes an arbitrary
                    length octet array salt <code>SALT</code> and an arbitrary length octet array
                    initial key <code>IKM</code>, to produce an octet array of arbitrary length. </para>
            </listitem>
            <listitem>
                <para>
                    <code>S(K, IV)</code> – A pseudo-random generator (stream cipher) which takes a
                        <code>S_KEY_LENGTH</code> byte octet array key <code>K</code> and a
                        <code>S_IV_LENGTH</code> byte octet array initialization vector
                        <code>IV</code> to produce an octet array key stream of arbitrary length. </para>
            </listitem>
            <listitem>
                <para>
                    <code>SPRP_Encrypt(K, M)/SPRP_Decrypt(K, M)</code> – A strong pseudo-random
                    permutation (SPRP) which takes a <code>SPRP_KEY_LENGTH</code> byte octet array
                    key <code>K</code> and arbitrary length message <code>M</code>, and produces the
                    encrypted ciphertext or decrypted plaintext respectively. </para>
                <para>When used with the default payload authentication mechanism, the SPRP MUST be
                    "fragile" in that any amount of modifications to <code>M</code> results in
                    a large number of unpredictable changes across the whole message upon a
                        <code>SPRP_Encrypt()</code> or <code>SPRP_Decrypt()</code>
                    operation. </para>
            </listitem>
            <listitem>
                <para>
                    <code>EXP(X, Y)</code> – An exponentiation function which takes the
                        <code>GROUP_ELEMENT_LENGTH</code> byte octet array group elements
                        <code>X</code> and <code>Y</code>, and returns <code>X ^^ Y</code> as a
                        <code>GROUP_ELEMENT_LENGTH</code> byte octet array. </para>
                <para>Let <code>G</code> denote the generator of the group, and
                        <code>EXP_KEYGEN()</code> return a
                        <code>GROUP_ELEMENT_LENGTH</code> byte octet array group element
                    usable as private key. </para>
                <para>The group defined by <code>G</code> and <code>EXP(X, Y)</code>
                    MUST satisfy the Decision Diffie-Hellman problem. </para>
            </listitem>
            <listitem>
                <para>
                    <code>EXP_KEYGEN()</code> – Returns a new "suitable" private key for
                        <code>EXP()</code>. </para>
            </listitem>
        </itemizedlist>
        <section xml:id="sphinx-key-derivation-function">
            <title>Sphinx key derivation function</title>
            <para>Sphinx packet creation and processing uses a common Key Derivation Function (KDF)
                to derive the required MAC and symmetric cryptographic keys from a per-hop shared
                secret. </para>
            <para>The output of the KDF is partitioned according to the following structure: </para>
            <programlisting>struct {
opaque header_mac[M_KEY_LENGTH];
opaque header_encryption[S_KEY_LENGTH];
opaque header_encryption_iv[S_IV_LENGTH];
opaque payload_encryption[SPRP_KEY_LENGTH]
opaque blinding_factor[GROUP_ELEMENT_LENGTH];
} SphinxPacketKeys;

Sphinx_KDF( info, shared_secret ) -&gt; packet_keys</programlisting>
            <para>Inputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>info</code> – The optional context and application specific
                        information. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>shared_secret</code> – The per-hop shared secret derived from the
                        Diffie-Hellman key exchange. </para>
                </listitem>
            </itemizedlist>
            <para>Outputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>packet_keys</code> The SphinxPacketKeys required to handle
                        packet creation or processing. </para>
                </listitem>
            </itemizedlist>
            <para>The output packet_keys is calculated as follows: </para>
            <programlisting>kdf_out = KDF( info, shared_secret )
packet_keys = kdf_out[:LEN( SphinxPacketKeys )]</programlisting>
        </section>
    </section>
    <section xml:id="sphinx-packet-parameters">
        <title>Sphinx packet parameters</title>
        <section xml:id="sphinx-parameter-constants">
            <title>Sphinx parameter constants</title>
            <para>The Sphinx packet Format is parameterized by the implementation based on the
                application and security requirements. </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>AD_LENGTH</code> – The constant amount of per-packet unencrypted
                        additional data in bytes. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>PAYLOAD_TAG_LENGTH</code> – The length of the message payload
                        authentication tag in bytes. This SHOULD be set to at least 16 bytes (128
                        bits). </para>
                </listitem>
                <listitem>
                    <para>
                        <code>PER_HOP_RI_LENGTH</code> – The length of the <xref
                            linkend="per-hop-routing-information"/> in bytes. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>NODE_ID_LENGTH</code> – The node identifier length in bytes. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>RECIPIENT_ID_LENGTH</code> – The recipient identifier length in bytes. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>SURB_ID_LENGTH</code> – The <link
                            linkend="single-use-reply-block-surb-creation">single use reply block
                        </link> identifier length in bytes. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>MAX_HOPS</code> – The maximum number of hops a packet can traverse. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>PAYLOAD_LENGTH</code> – The per-packet message payload length in
                        bytes, including a <code>PAYLOAD_TAG_LENGTH</code> byte authentication tag. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>KDF_INFO</code> – A constant opaque byte vector used as the info
                        parameter to the KDF for the purpose of domain separation. </para>
                </listitem>
            </itemizedlist>
        </section>
        <section xml:id="sphinx-packet-geometry">
            <title>Sphinx packet geometry</title>
            <para>The Sphinx packet geometry is derived from the <xref
                    linkend="sphinx-parameter-constants"/>. These are all derived parameters, and
                are primarily of interest to implementers. </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>ROUTING_INFO_LENGTH</code> – The total length of the "routing
                        information" Sphinx packet header component in bytes: </para>
                </listitem>
            </itemizedlist>
            <programlisting>ROUTING_INFO_LENGTH = PER_HOP_RI_LENGTH * MAX_HOPS</programlisting>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>HEADER_LENGTH</code> – The length of the Sphinx packet header in
                        bytes: </para>
                </listitem>
            </itemizedlist>
            <programlisting>HEADER_LENGTH = AD_LENGTH + GROUP_ELEMENT_LENGTH + ROUTING_INFO_LENGTH + MAC_LENGTH</programlisting>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>PACKET_LENGTH</code> – The length of the Sphinx packet in bytes: </para>
                </listitem>
            </itemizedlist>
            <programlisting>PACKET_LENGTH = HEADER_LENGTH + PAYLOAD_LENGTH</programlisting>
        </section>
    </section>
    <section xml:id="the-sphinx-cryptographic-packet-structure">
        <title>The Sphinx cryptographic packet Structure</title>
        <para>Each Sphinx packet consists of two parts: the Sphinx packet header and the Sphinx
            packet payload: </para>
        <programlisting>struct {
opaque header[HEADER_LENGTH];
opaque payload[PAYLOAD_LENGTH];
} SphinxPacket;</programlisting>
        <itemizedlist spacing="compact">
            <listitem>
                <para>
                    <code>header</code> – The packet header consists of several components, which
                    convey the information necessary to verify packet integrity and correctly
                    process the packet. </para>
            </listitem>
            <listitem>
                <para>
                    <code>payload</code>  – The application message data. </para>
            </listitem>
        </itemizedlist>
        <section xml:id="sphinx-packet-header">
            <title>Sphinx packet header</title>
            <para>The Sphinx packet header refers to the block of data immediately preceding the
                Sphinx packet payload in a Sphinx packet. </para>
            <para>The structure of the Sphinx packet header is defined as follows: </para>
            <programlisting>struct {
opaque additional_data[AD_LENGTH]; /* Unencrypted. */
opaque group_element[GROUP_ELEMENT_LENGTH];
opaque routing_information[ROUTING_INFO_LENGTH];
opaque MAC[MAC_LENGTH];
} SphinxHeader;</programlisting>
            <itemizedlist>
                <listitem>
                    <para>
                        <code>additional_data</code> – Unencrypted per-packet additional data (AD)
                        that is visible to every hop. The AD is authenticated on a per-hop basis. </para>
                    <para>As the additional_data is sent in the clear and traverses the network
                        unaltered, implementations MUST take care to ensure that the field cannot be
                        used to track individual packets. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>group_element</code> – An element of the cyclic group, used to derive
                        the per-hop key material required to authenticate and process the rest of
                        the SphinxHeader and decrypt a single layer of the Sphinx packet payload
                        encryption. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>routing_information</code> – A vector of per-hop routing information,
                        encrypted and authenticated in a nested manner. Each element of the vector
                        consists of a series of routing commands, specifying all of the information
                        required to process the packet. </para>
                    <para>The precise encoding format is specified in <xref
                            linkend="per-hop-routing-information"/>. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>MAC</code> – A message authentication code tag covering the
                        additional_data, group_element, and routing_information. </para>
                </listitem>
            </itemizedlist>
        </section>
        <section xml:id="per-hop-routing-information">
            <title>Per-hop routing information</title>
            <para>The routing information component of the Sphinx packet header contains a vector of
                per-hop routing information. When processing a packet, the per-hop processing is set
                up such that the first element in the vector contains the routing commands for the
                current hop. </para>
            <para>The structure of the routing information is as follows: </para>
            <programlisting>struct {
RoutingCommand routing_commands&lt;1..2^8-1&gt;; /* PER_HOP_RI_LENGTH bytes */
opaque encrypted_routing_commands[ROUTING_INFO_LENGTH - PER_HOP_RI_LENGTH];
} RoutingInformation;</programlisting>
            <para>The structure of a single <command>RoutingCommand</command> is as follows: </para>
            <programlisting>struct {
RoutingCommandType command;
select (RoutingCommandType) {
case null:               NullCommand;
case next_node_hop:      NextNodeHopCommand;
case recipient:          RecipientCommand;
case surb_reply:         SURBReplyCommand;
};
} RoutingCommand;</programlisting>
            <para>The following routing commands are currently defined: </para>
            <programlisting>enum {
null(0),
next_node_hop(1),
recipient(2),
surb_reply(3),

/* Routing commands between 0 and 0x7f are reserved. */

(255)
} RoutingCommandType;</programlisting>
            <para>The <command>NullCommand</command> structure is as follows: </para>
            <programlisting>struct {
opaque padding&lt;0..PER_HOP_RI_LENGTH-1&gt;;
} NullCommand;</programlisting>
            <para>The <command>NextNodeHopCommand</command> structure is as follows: </para>
            <programlisting>struct {
opaque next_hop[NODE_ID_LENGTH];
opaque MAC[MAC_LENGTH];
} NextNodeHopCommand;</programlisting>
            <para>The <command>RecipientCommand</command> structure is as follows: </para>
            <programlisting>struct {
opaque recipient[RECIPEINT_ID_LENGTH];
} RecipientCommand;</programlisting>
            <para>The <command>SURBReplyCommand</command> structure is as follows: </para>
            <programlisting>struct {
opaque id[SURB_ID_LENGTH];
} SURBReplyCommand;</programlisting>
            <para>While the <command>NullCommand</command> padding field is specified as opaque,
                implementations SHOULD zero fill the padding. The choice of <code>0x00</code>
                as the terminal <command>NullCommand</command> is deliberate to ease implementation,
                as <code>ZEROBYTES(N)</code> produces a valid <command>NullCommand</command>
                <command>RoutingCommand</command>, resulting in <quote>appending zero filled
                    padding</quote> producing valid output. </para>
            <para>Implementations MUST pad the <command>RoutingCommands</command> vector so that it
                is exactly <code>PER_HOP_RI_LENGTH</code> bytes, by appending a terminal
                    <command>NullCommand</command> if necessary. </para>
            <para>Every non-terminal hop’s <command>RoutingCommands</command> MUST include a
                        <command>NextNodeHopCommand</command>. </para>
        </section>
        <section xml:id="sphinx-packet-payload">
            <title>Sphinx packet payload</title>
            <para>The Sphinx packet payload refers to the block of data immediately following the
                Sphinx packet header in a Sphinx packet. </para>
            <para>For most purposes the structure of the Sphinx packet payload can be treated as a
                single contiguous byte vector of opaque data. </para>
            <para>Upon packet creation, the payload is repeatedly encrypted (unless it is a SURB
                reply as described in <xref linkend="single-use-reply-block-surb-creation"/>) using
                keys derived from the Diffie-Hellman key exchange between the packet's
                    <code>group_element</code> and the public key of each node in the path. </para>
            <para>Authentication of packet integrity is done by prepending a tag set to a known
                value to the plaintext prior to the first encrypt operation. By virtue of the
                fragile nature of the SPRP function, any alteration to the encrypted payload as it
                traverses the network will result in an irrecoverably corrupted plaintext when the
                payload is decrypted by the recipient. </para>
        </section>
    </section>
    <section xml:id="sphinx-packet-creation">
        <title>Sphinx packet creation</title>
        <para>For the sake of brevity, the pseudo-code for all of the operations will take a vector
            of the following PathHop structure as a parameter named <code>path[]</code> to specify
            the path a packet will traverse, along with the per-hop routing commands and per-hop
            public keys. </para>
        <programlisting>struct {
/* There is no need for a node_id here, as
routing_commands[0].next_hop specifies that
information for all non-terminal hops. */
opaque public_key[GROUP_ELEMENT_LENGTH];
RoutingCommand routing_commands&lt;1...2^8-1&gt;;
} PathHop;</programlisting>
        <para>It is assumed that each <command>RoutingCommands</command> vector except for the
            terminal entry contains at least a <command>RoutingCommand</command> consisting of a
            partially assembled <command>NextNodeHopCommand</command> with the <code>next_hop</code>
            element filled in with the identifier of the next hop. </para>
        <section xml:id="create-a-sphinx-packet-header">
            <title>Create a Sphinx packet header</title>
            <para>Both the creation of a Sphinx packet and the creation of a SURB requires the
                generation of a Sphinx packet header, so it is specified as a distinct operation. </para>
            <programlisting>Sphinx_Create_Header( additional_data, path[] ) -&gt; sphinx_header,
               payload_keys</programlisting>
            <para>Inputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>additional_data</code> – The additional data that is visible to every
                        node along the path in the header. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>path</code> – The vector of <code>PathHop</code> structures in hop
                        order, specifying the node id, public key, and routing commands for each
                        hop. </para>
                </listitem>
            </itemizedlist>
            <para>Outputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para><code>sphinx_header</code> – The resulting Sphinx packet header. </para>
                </listitem>
                <listitem>
                    <para><code>payload_keys</code> – The vector of SPRP keys used to encrypt the
                        Sphinx packet payload, in hop order. </para>
                </listitem>
            </itemizedlist>
            <para>The <code>Sphinx_Create_Header</code> operation consists of the following
                steps: </para>
            <orderedlist numeration="arabic" spacing="compact">
                <listitem>
                    <para>Derive the key material for each hop. </para>
                </listitem>
            </orderedlist>
            <programlisting>num_hops = route.len
route_keys = [ ]
route_group_elements = [ ]
priv_key = EXP_KEYGEN()

/* Calculate the key material for the 0th hop. */
group_element = EXP( G, priv_key )
route_group_elements += group_element
shared_secret = EXP( path[0].public_key, priv_key )
route_keys += Sphinx_KDF( KDF_INFO, shared_secret )
blinding_factor = keys[0].blinding_factor

/* Calculate the key material for rest of the hops. */
for i = 1; i &lt; num_hops; ++i:
shared_secret = EXP( path[i].public_key, priv_key )
for j = 0; j &lt; i; ++j:
shared_secret = EXP( shared_secret, keys[j].blinding_factor )
route_keys += Sphinx_KDF( KDF_INFO, shared_secret )
group_element = EXP( group_element, keys[i-1].blinding_factor )
route_group_elements += group_element</programlisting>
            <para>At the conclusion of the derivation process: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>route_keys</code> – A vector of per-hop SphinxKeys. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>route_group_elements</code> – A vector of per-hop group elements. </para>
                </listitem>
            </itemizedlist>
            <orderedlist>
                <listitem>
                    <para>Derive the <code>routing_information</code> keystream and encrypted
                        padding for each hop. <programlisting>ri_keystream = [ ]
ri_padding = [ ]

for i = 0; i &lt; num_hops; ++i:
keystream = ZEROBYTES( ROUTING_INFO_LENGTH + PER_HOP_RI_LENGTH ) ^
S( route_keys[i].header_encryption,
route_keys[i].header_encryption_iv )
ks_len = LEN( keystream ) - (i + 1) * PER_HOP_RI_LENGTH

padding = keystream[ks_len:]
if i &gt; 0:
prev_pad_len = LEN( ri_padding[i-1] )
padding = padding[:prev_pad_len] ^ ri_padding[i-1] |
padding[prev_pad_len]

ri_keystream += keystream[:ks_len]
ri_padding += padding

At the conclusion of the derivation process:
ri_keystream - A vector of per-hop routing_information
encryption keystreams.
ri_padding   - The per-hop encrypted routing_information
padding.</programlisting></para>
                </listitem>
                <listitem>
                    <para>Create the <command>routing_information</command>
                        block.<programlisting>/* Start with the terminal hop, and work backwards. */
i = num_hops - 1

/* Encode the terminal hop's routing commands. As the
terminal hop can never have a NextNodeHopCommand, there
are no per-hop alterations to be made. */
ri_fragment = path[i].routing_commands |
ZEROBYTES( PER_HOP_RI_LENGTH - LEN( path[i].routing_commands ) )

/* Encrypt and MAC. */
ri_fragment ^= ri_keystream[i]
mac = MAC( route_keys[i].header_mac, additional_data |
route_group_elements[i] | ri_fragment |
ri_padding[i-1] )
routing_info = ri_fragment
if num_hops &lt; MAX_HOPS:
pad_len = (MAX_HOPS - num_hops) * PER_HOP_RI_LENGTH
routing_info = routing_info | RNG( pad_len )

/* Calculate the routing info for the rest of the hops. */
for i = num_hops - 2; i &gt;= 0; --i:
cmds_to_encode = [ ]

/* Find and finalize the NextNodeHopCommand. */
for j = 0; j &lt; LEN( path[i].routing_commands; j++:
cmd = path[i].routing_commands[j]
if cmd.command == next_node_hop:
/* Finalize the NextNodeHopCommand. */
cmd.MAC = mac
cmds_to_encode = cmds_to_encode + cmd /* Append */

/* Append a terminal NullCommand. */
ri_fragment = cmds_to_encode |
ZEROBYTES( PER_HOP_RI_LENGTH - LEN( cmds_to_encode ) )

/* Encrypt and MAC */
routing_info = ri_fragment | routing_info /* Prepend. */
routing_info ^= ri_keystream[i]
if i &gt; 0:
mac = MAC( route_keys[i].header_mac, additional_data |
route_group_elements[i] | routing_info |
ri_padding[i-1] )
else:
mac = MAC( route_keys[i].header_mac, additional_data |
route_group_elements[i] | routing_info )

At the conclusion of the derivation process:
routing_info - The completed routing_info block.
mac          - The MAC for the 0th hop.</programlisting></para>
                </listitem>
                <listitem>
                    <para>Assemble the completed Sphinx packet header and Sphinx packet payload SPRP
                        key vector. </para>
                    <programlisting>/* Assemble the completed Sphinx packet header. */
SphinxHeader sphinx_header
sphinx_header.additional_data = additional_data
sphinx_header.group_element = route_group_elements[0] /* From step 1. */
sphinx_header.routing_info = routing_info   /* From step 3. */
sphinx_header.mac = mac                     /* From step 3. */

/* Preserve the Sphinx Payload SPRP keys, to return to the
caller. */
payload_keys = [ ]
for i = 0; i &lt; nr_hops; ++i:
payload_keys += route_keys[i].payload_encryption

At the conclusion of the assembly process:
sphinx_header - The completed sphinx_header, to be returned.
payload_keys  - The vector of SPRP keys, to be returned.</programlisting>
                </listitem>
            </orderedlist>
        </section>
        <section xml:id="create-a-sphinx-packet">
            <title>Create a Sphinx packet</title>
            <programlisting>Sphinx_Create_Packet( additional_data, path[], payload ) -&gt; sphinx_packet</programlisting>
            <para>Inputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>additional_data</code> – The additional data that is visible to every
                        node along the path in the header. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>path</code> – The vector of PathHop structures in hop order,
                        specifying the node id, public key, and routing commands for each hop. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>payload</code> – The packet payload message plaintext. </para>
                </listitem>
            </itemizedlist>
            <para>Outputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>sphinx_packet</code> – The resulting Sphinx packet. </para>
                </listitem>
            </itemizedlist>
            <para>The <code>Sphinx_Create_Packet</code> operation consists of the following
                steps: </para>
            <orderedlist numeration="arabic" spacing="compact">
                <listitem>
                    <para>Create the Sphinx packet header and SPRP key vector.
                        <programlisting>sphinx_header, payload_keys =
Sphinx_Create_Header( additional_data, path )</programlisting></para>
                </listitem>
                <listitem>
                    <para>Prepend the authentication tag, and append padding to the
                        payload.<programlisting>payload = ZERO_BYTES( PAYLOAD_TAG_LENGTH ) | payload
payload = payload | ZERO_BYTES( PAYLOAD_LENGTH - LEN( payload ) )</programlisting></para>
                </listitem>
                <listitem>
                    <para>Encrypt the payload.
                        <programlisting>for i = nr_hops - 1; i &gt;= 0; --i:
payload = SPRP_Encrypt( payload_keys[i], payload )</programlisting></para>
                </listitem>
                <listitem>
                    <para>Assemble the completed Sphinx packet.
                        <programlisting>SphinxPacket sphinx_packet
sphinx_packet.header = sphinx_header
sphinx_packet.payload = payload</programlisting></para>
                </listitem>
            </orderedlist>
        </section>
    </section>
    <section xml:id="sphinx-packet-processing">
        <title>Sphinx packet processing</title>
        <para>Mix nodes process incoming packets first by performing the <code>Sphinx_Unwrap</code>
            operation to authenticate and decrypt the packet, and, if applicable, prepare the packet
            to be forwarded to the next node. </para>
        <para>If <code>Sphinx_Unwrap</code> returns an error for any given packet, the packet
            MUST be discarded with no additional processing. </para>
        <para>After a packet has been unwrapped successfully, a replay detection tag is checked to
            ensure that the packet has not been seen before. If the packet is a replay, the packet
            MUST be discarded with no additional processing. </para>
        <para>The routing commands for the current hop are interpreted and executed, and finally
            the packet is forwarded to the next mix node over the network or presented to the
            application if the current node is the final recipient. </para>
        <section xml:id="sphinx_unwrap-operation">
            <title>Sphinx_Unwrap operation</title>
            <para>The <code>Sphinx_Unwrap</code> operation contains the majority of the per-hop
                packet processing, handling authentication, decryption, and modifying the packet
                prior to forwarding it to the next node. </para>
            <programlisting>Sphinx_Unwrap( routing_private_key, sphinx_packet ) -&gt; sphinx_packet,
                  routing_commands,
                  replay_tag</programlisting>
            <para>Inputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para><code>private_routing_key</code> – A group element
                            <code>GROUP_ELEMENT_LENGTH</code> bytes in length that contains the
                        unwrapped content of the mix node’s private key. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>sphinx_packet</code> – A Sphinx packet to unwrap. </para>
                </listitem>
            </itemizedlist>
            <para>Outputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>error</code> – Indicating a unsuccessful unwrap operation if
                        applicable. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>sphinx_packet</code> – The resulting Sphinx packet. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>routing_commands</code> – A vector of RoutingCommand, specifying the
                        post unwrap actions to be taken on the packet. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>replay_tag</code> – A tag used to detect whether this packet was
                        processed before. </para>
                </listitem>
            </itemizedlist>
            <para>The <code>Sphinx_Unwrap</code> operation consists of the following steps: </para>
            <orderedlist numeration="arabic" spacing="compact">
                <listitem override="0">
                    <para>(Optional) Examine the Sphinx packet header’s additional data.
                        <programlisting>hdr = sphinx_packet.header
shared_secret = EXP( hdr.group_element, private_routing_key )
replay_tag = H( shared_secret )</programlisting></para>
                    <para>If the header’s <code>additional_data</code> element contains information
                        required to complete the unwrap operation, such as specifying the packet
                        format version or the cryptographic primitives used examine it now. </para>
                    <para>Implementations MUST NOT treat the information in the
                            <code>additional_data</code> element as trusted until after the
                        completion of
                        <?oxy_custom_start type="oxy_content_highlight" color="255,255,0"?>Step 3
                            (<quote>Validate the Sphinx packet
                        header</quote><?oxy_custom_end?>).</para>
                </listitem>
                <listitem>
                    <para>Derive the various keys required for packet processing.
                        <programlisting>keys = Sphinx_KDF( KDF_INFO, shared_secret )</programlisting></para>
                </listitem>
                <listitem>
                    <para>Validate the Sphinx packet header.
                        <programlisting>derived_mac = MAC( keys.header_mac, hdr.additional_data |
hdr.group_element |
hdr.routing_information )
if !CONSTANT_TIME_CMP( derived_mac, hdr.MAC):
/* MUST abort processing if the header is invalid. */
return ErrorInvalidHeader</programlisting></para>
                </listitem>
                <listitem>
                    <para>Extract the per-hop routing commands for the current
                        hop.<programlisting>/* Append padding to preserve length-invariance, as the routing
commands for the current hop will be removed. */
padding = ZEROBYTES( PER_HOP_RI_LENGTH )
B = hdr.routing_information | padding

/* Decrypt the entire routing_information block. */
B = B ^ S( keys.header_encryption, keys.header_encryption_iv ) </programlisting></para>
                </listitem>
                <listitem>
                    <para>Parse the per-hop routing commands.
                        <programlisting>cmd_buf = B[:PER_HOP_RI_LENGTH]
new_routing_information = B[PER_HOP_RI_LENGTH:]

next_mix_command_idx = -1
routing_commands = [ ]
for idx = 0; idx &lt; PER_HOP_RI_LENGTH {
/* WARNING: Bounds checking omitted for brevity. */
cmd_type = b[idx]
cmd = NULL
switch cmd_type {
case null: goto done  /* No further commands. */

case next_node_hop:
cmd = RoutingCommand( B[idx:idx+1+LEN( NextNodeHopCommand )] )
next_mix_command_idx = i /* Save for step 7. */
idx += 1 + LEN( NextNodeHopCommand )
break

case recipient:
cmd = RoutingCommand( B[idx:idx+1+LEN( FinalDestinationCommand )] )
idx += 1 + LEN( RecipientCommand )
break

case surb_reply:
cmd = RoutingCommand( B[idx:idx+1+LEN( SURBReplyCommand )] )
idx += 1 + LEN( SURBReplyCommand )
break

default:
/* MUST abort processing on unrecognized commands. */
return ErrorInvalidCommand
}
routing_commands += cmd /* Append cmd to the tail of the list. */
}
done:</programlisting></para>
                </listitem>
                <listitem>
                 <para>Apply this hop's <command>routing_commands</command> (a vector of
                            <command>SphinxRoutingCommand</command>) and decrypt the Sphinx packet
                        payload.<programlisting>payload = sphinx_packet.payload
payload = SPRP_Decrypt( key.payload_encryption, payload )
sphinx_packet.payload = payload </programlisting></para>
                </listitem>
                <listitem><para>If the <command>routing_commands</command> vector included
                            a<command>NextNodeHopCommand</command>, transform the packet for
                        forwarding to the next mix node by applying
                            <code>new_routing_information</code>.<programlisting>if next_mix_command_idx != -1:
cmd = routing_commands[next_mix_command_idx]
hdr.group_element = EXP( hdr.group_element, keys.blinding_factor )
hdr.routing_information = new_routing_information
hdr.mac = cmd.MAC
sphinx_packet.hdr = hdr</programlisting></para>
                
             </listitem>

            
            </orderedlist>
            


            <section xml:id="post-sphinx_unwrap-processing">
                <title>Post-Sphinx_Unwrap processing</title>
                <para>Upon the completion of the <code>Sphinx_Unwrap</code> operation,
                    implementations MUST take several additional steps. As the exact behavior is
                    mostly implementation specific, pseudo-code will not be provided for most of the
                    post processing steps. </para>
                <orderedlist numeration="arabic" spacing="compact">
                    <listitem>
                        <para>Apply replay detection to the packet.</para>
                        <para>The <code>replay_tag</code> value returned by
                                <code>Sphinx_Unwrap</code> MUST be unique across all packets
                            processed with a given <code>private_routing_key</code>. </para>
                        <para>The exact specifics of how to detect replays is left up to the
                            implementation, however any replays that are detected MUST be discarded
                            immediately. </para>
                    </listitem>
                    <listitem>
                        <para>Act on the routing commands, if any. </para>
                        <para>The exact specifics of how implementations chose to apply routing
                            commands is deliberately left unspecified, however in general: </para>
                        <itemizedlist>
                            <listitem>
                                <para>If there is a <command>NextNodeHopCommand</command>, the
                                    packet should be forwarded to the next node based on the
                                        <code>next_hop</code> field upon completion of the post
                                    processing. </para>
                                <para>The lack of a <command>NextNodeHopCommand</command> indicates
                                    that the packet is destined for the current node. </para>
                            </listitem>
                            <listitem>
                                <para>If there is a <command>SURBReplyCommand</command>, the packet
                                    should be treated as a <code>SURBReply</code> destined for the
                                    current node, and decrypted accordingly as described in <xref
                                        linkend="decrypt-a-sphinx-reply-originating-from-a-surb"
                                    />.</para>
                            </listitem>
                            <listitem>
                                <para>If the implementation supports multiple recipients on a single
                                    node, the <command>RecipientCommand</command> command should be
                                    used to determine the correct recipient for the packet, and the
                                    payload delivered as appropriate. </para>
                                <para>It is possible for both a <command>RecipientCommand</command>
                                    and a <command>NextNodeHopCommand</command> to be present
                                    simultaneously in the routing commands for a given hop. The
                                    behavior when this situation occurs is implementation defined.
                                </para>
                            </listitem>
                        </itemizedlist>
                    </listitem>
                    <listitem>
                        <para>Authenticate the packet if required.</para>
                        <para>If the packet is destined for the current node, the integrity of the
                            payload MUST be authenticated. </para>
                        <para>The authentication is done as follows: </para>
                        <programlisting>derived_tag = sphinx_packet.payload[:PAYLOAD_TAG_LENGTH]
expected_tag = ZEROBYTES( PAYLOAD_TAG_LENGTH )
if !CONSTANT_TIME_CMP( derived_tag, expected_tag ):
/* Discard the packet with no further processing. */
return ErrorInvalidPayload</programlisting>
                        <para>Remove the authentication tag before presenting the payload to the
                            application. </para>
                        <programlisting>sphinx_packet.payload = sphinx_packet.payload[PAYLOAD_TAG_LENGTH:]</programlisting>
                    </listitem>
                </orderedlist>
            </section>
        </section>
        <section xml:id="single-use-reply-block-surb-creation">
            <title>Single-use reply block (SURB) creation</title>
            <para>A single use reply block (SURB) is a delivery token with a short lifetime, that
                can be used by the recipient to reply to the initial sender. </para>
            <para>SURBs allow for anonymous replies, when the recipient does not know the sender of
                the message. Usage of SURBs guarantees anonymity properties but also makes the reply
                messages indistinguishable from forward messages both to external adversaries as
                well as the mix nodes. </para>
            <para>When a SURB is created, a matching reply block decryption token is created, which
                is used to decrypt the reply message that is produced and delivered via the SURB. </para>
            <para>The Sphinx SURB wire encoding is implementation defined, but for the purposes of
                illustrating creation and use, the following will be used: </para>
            <programlisting>struct {
SphinxHeader sphinx_header;
opaque first_hop[NODE_ID_LENGTH];
opaque payload_key[SPRP_KEY_LENGTH];
} SphinxSURB;</programlisting>
            <section xml:id="create-a-sphinx-surb-and-decryption-token">
                <title>Create a Sphinx SURB and decryption token</title>
                <para>Structurally a SURB consists of three parts, a pre-generated Sphinx packet
                    header, a node identifier for the first hop to use when using the SURB to reply,
                    and cryptographic keying material by which to encrypt the reply’s payload. All
                    elements must be securely transmitted to the recipient, perhaps as part of a
                    forward Sphinx packet's Payload, but the exact specifics on how to accomplish
                    this is left up to the implementation. </para>
                <para>When creating a SURB, the terminal routing_commands vector SHOULD include a
                        <command>SURBReplyCommand</command>, containing an identifier to ensure that
                    the payload can be decrypted with the correct set of keys (decryption token).
                    The routing command is left optional, as it is conceivable that implementations
                    may chose to use trial decryption, and or limit the number of outstanding SURBs
                    to solve this problem. </para>
                <programlisting>Sphinx_Create_SURB( additional_data, first_hop, path[] ) -&gt;
             sphinx_surb,
             decryption_token</programlisting>
                <para>Inputs: </para>
                <itemizedlist spacing="compact">
                    <listitem>
                        <para>
                            <code>additional_data</code> – The additional data that is visible to
                            every node along the path in the header. </para>
                    </listitem>
                    <listitem>
                        <para>
                            <code>first_hop</code> – The node id of the first hop the recipient must
                            use when replying via the SURB. </para>
                    </listitem>
                    <listitem>
                        <para>
                            <code>path</code> – The vector of <code>PathHop</code> structures in hop
                            order, specifying the node id, public key, and routing commands for each
                            hop. </para>
                    </listitem>
                </itemizedlist>
                <para>Outputs: </para>
                <itemizedlist spacing="compact">
                    <listitem>
                        <para>
                            <code>sphinx_surb</code> – The resulting Sphinx SURB. </para>
                    </listitem>
                    <listitem>
                        <para>
                            <code>decryption_token</code> – The decryption token associated with the
                            SURB. </para>
                    </listitem>
                </itemizedlist>
                <para>The <code>Sphinx_Create_SURB</code> operation consists of the following steps: </para>
                <orderedlist numeration="arabic" spacing="compact">
                    <listitem>
                        <para>Create the Sphinx packet header and SPRP key
                            vector.<programlisting>sphinx_header, payload_keys =
Sphinx_Create_Header( additional_data, path )</programlisting></para>
                    </listitem>
                    <listitem>
                        <para>Create a key for the final layer of encryption.
                            <programlisting>final_key = RNG( SPRP_KEY_LENGTH )</programlisting></para>
                    </listitem>
                    <listitem>
                        <para>Build the SURB and Decryption
                            Token.<programlisting>SphinxSURB sphinx_surb;
sphinx_surb.sphinx_header = sphinx_header
sphinx_surb.first_hop = first_hop
sphinx_surb.payload_key = final_key

decryption_token = final_key + payload_keys /* Prepend */</programlisting></para>
                    </listitem>
                </orderedlist>
            </section>
            <section xml:id="decrypt-a-sphinx-reply-originating-from-a-surb">
                <title>Decrypt a Sphinx reply originating from a SURB</title>
                <para>A Sphinx reply packet that was generated using a SURB is externally
                    indistinguishable from a forward Sphinx packet as it traverses the network.
                    However, the recipient of the reply has an additional decryption step, the
                    packet starts off unencrypted, and accumulates layers of Sphinx packet payload
                    decryption as it traverses the network. </para>
                <para>Determining which decryption token to use when decrypting the SURB reply can
                    be done via the <command>SURBReplyCommand</command>’s id field, if one is
                    included at the time of the SURB’s creation. </para>
                <programlisting>Sphinx_Decrypt_SURB_Reply( decryption_token, payload ) -&gt; message</programlisting>
                <para>Inputs: </para>
                <itemizedlist spacing="compact">
                    <listitem>
                        <para>
                            <code>decryption_token</code> – The vector of keys allowing a client to
                            decrypt the reply ciphertext payload. This decryption_token is generated
                            when the SURB is created. </para>
                    </listitem>
                    <listitem>
                        <para>
                            <code>payload</code> – The Sphinx packet ciphertext payload. </para>
                    </listitem>
                </itemizedlist>
                <para>Outputs: </para>
                <itemizedlist spacing="compact">
                    <listitem>
                        <para>
                            <code>error</code> – Indicating a unsuccessful unwrap operation if
                            applicable. </para>
                    </listitem>
                    <listitem>
                        <para>
                            <code>message</code> – The plaintext message. </para>
                    </listitem>
                </itemizedlist>
                <para>The <code>Sphinx_Decrypt_SURB_Reply</code> operation consists of the following
                    steps: </para>
                <orderedlist numeration="arabic" spacing="compact">
                    <listitem override="2">
                        <para>Encrypt the message to reverse the decrypt operations the payload
                            acquired as it traversed the network.
                            <programlisting>for i = LEN( decryption_token ) - 1; i &gt; 0; --i:
payload = SPRP_Encrypt( decryption_token[i], payload )</programlisting></para>
                    </listitem>
                    <listitem>
                        <para>Decrypt and authenticate the message ciphertext.
                            <programlisting>message = SPRP_Decrypt( decryption_token[0], payload )

derived_tag = message[:PAYLOAD_TAG_LENGTH]
expected_tag = ZEROBYTES( PAYLOAD_TAG_LENGTH )
if !CONSTANT_TIME_CMP( derived_tag, expected_tag ):
return ErrorInvalidPayload

message = message[PAYLOAD_TAG_LENGTH:]</programlisting></para>
                    </listitem>
                </orderedlist>
            </section>
        </section>
        <section xml:id="single-use-reply-block-replies">
            <title>SURB replies</title>
            <para>The process for using a SURB to reply anonymously is slightly different from the
                standard packet creation process, as the Sphinx packet header is already generated
                (as part of the SURB), and there is an additional layer of Sphinx packet payload
                encryption that must be performed. </para>
            <programlisting>Sphinx_Create_SURB_Reply( sphinx_surb, payload ) -&gt; sphinx_packet</programlisting>
            <para>Inputs: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>
                        <code>sphinx_surb</code> – The <code>SphinxSURB</code> structure, decoded
                        from the implementation defined wire encoding. </para>
                </listitem>
                <listitem>
                    <para>
                        <code>payload</code> – The packet payload message plaintext. </para>
                </listitem>
            </itemizedlist>
            <para>The <code>Sphinx_Create_SURB_Reply</code> operation consists of the following
                steps: </para>
            <orderedlist numeration="arabic" spacing="compact">
                <listitem>
                    <para>Prepend the authentication tag, and append padding to the
                        payload.<programlisting>payload = ZERO_BYTES( PAYLOAD_TAG_LENGTH ) | payload
payload = payload | ZERO_BYTES( PAYLOAD_LENGTH - LEN( payload ) ) </programlisting></para>
                </listitem>
                <listitem>
                    <para>Encrypt the
                        payload.<programlisting>payload = SPRP_Encrypt( sphinx_surb.payload_key, payload )</programlisting></para>
                </listitem>
                <listitem>
                    <para>Assemble the completed Sphinx packet.
                        <programlisting>SphinxPacket sphinx_packet
sphinx_packet.header = sphinx_surb.sphinx_header
sphinx_packet.payload = payload</programlisting></para>
                </listitem>
            </orderedlist>
            <para>The completed <code>sphinx_packet</code> MUST be sent to the node specified
                via <code>sphinx_surb.node_id</code>, as the entire reply
                    <code>sphinx_packet</code>’s header is pre-generated. </para>
        </section>
        <section xml:id="anonymity-considerations">
            <title>Anonymity considerations</title>
            <section xml:id="optional-non-constant-length-sphinx-packet-header-padding">
                <title>Optional non-constant length Sphinx packet header padding</title>
                <para>Depending on the mix topology, there is no hard requirement that the per-hop
                    routing info is padded to one fixed constant length. </para>
                <para>For example, assuming a layered topology (referred to as stratified topology
                    in the literature) <link linkend="MIXTOPO10">MIXTOPO10</link>, where the layer
                    of any given mix node is public information, as long as the following two
                    invariants are maintained, there is no additional information available to an
                    adversary: </para>
                <orderedlist numeration="arabic" spacing="compact">
                    <listitem>
                        <para>All packets entering any given mix node in a certain layer are
                            uniform in length. </para>
                    </listitem>
                    <listitem>
                        <para>All packets leaving any given mix node in a certain layer are uniform
                            in length. </para>
                    </listitem>
                </orderedlist>
                <para>The only information available to an external or internal observer is the
                    layer of any given mix node (via the packet length), which is information they
                    are assumed to have by default in such a design. </para>
            </section>
            <section xml:id="additional-data-field-considerations">
                <title>Additional data field considerations</title>
                <para>The Sphinx packet construct is crafted such that any given packet is bitwise
                    unlinkable after a <code>Sphinx_Unwrap</code> operation, provided that the
                    optional additional data (AD) facility is not used. This property ensures that
                    external passive adversaries are unable to track a packet based on content as it
                    traverses the network. As the on-the-wire AD field is static through the
                    lifetime of a packet (that is, left unaltered by the <code>Sphinx_Unwrap</code>
                    operation), implementations and applications that wish to use this facility MUST
                    NOT transmit AD that can be used to distinctly identify individual packets. </para>
            </section>
            <section xml:id="forward-secrecy-considerations">
                <title>Forward secrecy considerations</title>
                <para>Each node acting as a mix MUST regenerate their asymmetric key pair relatively
                    frequently. Upon key rotation the old private key MUST be securely destroyed. As
                    each layer of a Sphinx packet is encrypted via key material derived from the
                    output of an ephemeral/static Diffie-Hellman key exchange, without the rotation,
                    the construct does not provide Perfect Forward Secrecy. Implementations SHOULD
                    implement defense-in-depth mitigations, for example by using strongly
                    forward-secure link protocols to convey Sphinx packets between nodes. </para>
                <para>This frequent mix routing key rotation can limit SURB usage by directly
                    reducing the lifetime of SURBs. In order to have a strong Forward Secrecy
                    property while maintaining a higher SURB lifetime, designs such as forward
                    secure mixes <link linkend="SFMIX03">SFMIX03</link> could be used. </para>
            </section>
            <section xml:id="compulsion-threat-considerations">
                <title>Compulsion threat considerations</title>
                <para>Reply Blocks (SURBs), forward and reply Sphinx packets are all vulnerable to
                    the compulsion threat, if they are captured by an adversary. The adversary can
                    request iterative decryptions or keys from a series of honest mixes in order to
                    perform a deanonymizing trace of the destination. </para>
                <para>While a general solution to this class of attacks is beyond the scope of this
                    document, applications that seek to mitigate or resist compulsion threats could
                    implement the defenses proposed in <link linkend="COMPULS05">COMPULS05</link>
                    via a series of routing command extensions. </para>
            </section>
            <section xml:id="surb-usage-considerations-for-volunteer-operated-mix-networks">
                <title>SURB usage considerations for volunteer-operated mix networks</title>
                <para>Given a hypothetical scenario where Alice and Bob both wish to keep their
                    location on the mix network hidden from the other, and Alice has somehow
                    received a SURB from Bob, Alice MUST not utilize the SURB directly because in
                    the volunteer operated mix network the first hop specified by the SURB could be
                    operated by Bob for the purpose of deanonymizing Alice. </para>
                <para>This problem could be solved via the incorporation of a "cross-over point"
                    such as that described in <link linkend="MIXMINION">MIXMINION</link>, for
                    example by having Alice delegating the transmission of a SURB Reply to a
                    randomly selected cross-over point in the mix network, so that if the first hop
                    in the SURB’s return path is a malicious mix, the only information gained is the
                    identity of the cross-over point. </para>
            </section>
        </section>
        <section xml:id="security-considerations">
            <title>Security considerations</title>
            <section xml:id="sphinx-payload-encryption-considerations">
                <title>Sphinx payload encryption considerations</title>
                <para>The payload encryption’s use of a fragile (non-malleable) SPRP is deliberate
                    and implementations SHOULD NOT substitute it with a primitive that does not
                    provide such a property (such as a stream cipher based PRF). In particular there
                    is a class of correlation attacks (tagging attacks) targeting anonymity systems
                    that involve modification to the ciphertext that are mitigated if alterations to
                    the ciphertext result in unpredictable corruption of the plaintext (avalanche
                    effect). </para>
                <para>Additionally, as the PAYLOAD_TAG_LENGTH based tag-then-encrypt payload
                    integrity authentication mechanism is predicated on the use of a non-malleable
                    SPRP, implementations that substitute a different primitive MUST authenticate
                    the payload using a different mechanism. </para>
                <para>Alternatively, extending the MAC contained in the Sphinx packet header to
                    cover the Sphinx packet payload will both defend against tagging attacks and
                    authenticate payload integrity. However, such an extension does not work with
                    the SURB construct presented in this specification, unless the SURB is only used
                    to transmit payload that is known to the creator of the SURB. </para>
            </section>
        </section>
    </section>
    <section xml:id="appendix-a.-references">
        <title>References</title>
        &COMPULS05;
        &MIXMINION;
        &MIXTOPO10;
        &RFC2119;
        &RFC5246;        
        &SFMIX03;
        &SPHINX09;
    </section>
</section>
