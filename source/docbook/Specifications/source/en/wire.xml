<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="http://docbook.org/xml/5.1/rng/docbook.rng" schematypens="http://relaxng.org/ns/structure/1.0"?>
<?xml-model href="http://docbook.org/xml/5.1/sch/docbook.sch" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>

<!-- The XSL transform inserts these values as Hugo frontmatter. -->
<!-- Additionally, a "date" timestamp is inserted by the stylesheet. -->

<?title "" ?> <!-- Required ... copy section title here [https://gohugo.io/content-management/front-matter/#title] -->
<?linkTitle "Wire protocol" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#linktitle] -->
<?description "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#description] -->
<?author "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#params] -->
<?url "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#url] -->
<?draft "false" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#draft] -->
<?slug "wireprotocol" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#slug] -->
<?layout "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#layout] -->
<?type "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#type] -->
<?weight "65" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#weight] -->
<?version "" ?> <!-- Optional [?] -->

<!DOCTYPE section [
    <!ENTITY % shared-content SYSTEM "/home/dwrob/git/website/source/docbook/shared-content.ent">
        %shared-content;
    <!ENTITY % references SYSTEM "/home/dwrob/git/website/source/docbook/references.ent">
        %references;
    <!ENTITY % terms SYSTEM "/home/dwrob/git/website/source/docbook/terms.ent">
        %terms;    
    <!ELEMENT xi:include (xi:fallback?) > 
    <!ATTLIST xi:include 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" 
    href CDATA #REQUIRED 
    parse (xml|text) "xml" 
    xpointer CDATA #IMPLIED 
    encoding CDATA #IMPLIED 
    accept CDATA #IMPLIED 
    accept-charset CDATA #IMPLIED 
    accept-language CDATA #IMPLIED > 
    <!ELEMENT xi:fallback ANY > 
    <!ATTLIST xi:fallback 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" > 
    <!ENTITY % local.preface.class "| xi:include"> 
    <!ENTITY % local.part.class "| xi:include"> 
    <!ENTITY % local.chapter.class "| xi:include"> 
    <!ENTITY % local.divcomponent.mix "| xi:include"> 
    <!ENTITY % local.para.char.mix "| xi:include"> 
    <!ENTITY % local.info.class "| xi:include"> 
    <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink"
    version="5.1" xml:id="wire_protocol">

    <title xml:id="wire_protocol.title">Mix network wire protocol specification</title>
    <info>
        <authorgroup>
            <author>
                <personname>
                    <firstname>Yawning</firstname>
                    <surname>Angel</surname>
                </personname>
            </author>
            <author>
                <personname>
                    <firstname>David</firstname>
                    <surname>Stainton</surname>
                </personname>
            </author>
        </authorgroup>
        <abstract>
            <para>This document defines the &program_name; mix network wire protocol for use in all
                network communications to, from, and within the &program_name; Mix Network.</para>
        </abstract>
    </info>
    <section xml:id="conventions-used-in-this-document">
        <title>Conventions used in this document</title>
        <para>The key words <quote>MUST</quote>, <quote>MUST NOT</quote>, <quote>REQUIRED</quote>,
                <quote>SHALL</quote>, <quote>SHALL NOT</quote>, <quote>SHOULD</quote>, <quote>SHOULD
                NOT</quote>, <quote>RECOMMENDED</quote>, <quote>MAY</quote>, and
                <quote>OPTIONAL</quote> in this document are to be interpreted as described in <xref
                linkend="RFC2119"/>. </para>
        <para>The <quote>C</quote> style Presentation Language as described in <xref
                linkend="RFC5246"/> Section 4 is used to represent data structures, except for
            cryptographic attributes, which are specified as opaque byte vectors. </para>
        <para>
            <code>x | y</code> denotes the concatenation of x and y. </para>
    </section>

    <section xml:id="wire_protocol_introduction">
        <title xml:id="wire_protocol_introduction.title">Introduction</title>
        <para>The &program_name; mix network wire protocol (KMNWP) is the custom wire protocol for
            all network communications to, from, and within the &program_name; mix network. This
            protocol provides mutual authentication along with an additional layer of cryptographic
            security and forward secrecy. </para>
        <section xml:id="key-encapsulation-mechanism">
            <title>Key encapsulation mechanism</title>
            <para>This protocol uses ANY key encapsulation mechanism. However, it is recommended
                that most users select a hybrid post-quantum KEM such as Xwing. <xref
                    linkend="XWING"/>
            </para>
        </section>
    </section>
    <section xml:id="core-protocol">
        <title>Core protocol</title>
        <para>The protocol is based on Kyber and Trevor Perrin’s Noise protocol framework <xref
                linkend="NOISE"/> along with <quote>Post Quantum Noise</quote> paper <link
                linkend="PQNOISE">PQNOISE</link>. Older previous versions of our transport were
            based on <link linkend="NOISEHFS">NOISEHFS</link>. </para>
        <para>Our transport protocol begins with a prologue, Noise handshake, followed by a stream
            of Noise transport messages in a minimal framing layer, over a TCP/IP connection. </para>
        <para>Our Noise protocol is configurable via the KEM selection in the TOML configuration
            files, here’s an example PQ Noise protocol string: </para>
        <programlisting>Noise_pqXX_Xwing_ChaChaPoly_BLAKE2b</programlisting>
        <para>The protocol string is a very condensed description of our protocol. We use the pqXX
            two-way Noise pattern which is described as follows: </para>
        <programlisting>pqXX: -&gt; e &lt;- ekem, s -&gt; skem, s &lt;- skem</programlisting>
        <para>The next part of the protocol string specifies the KEM, <code>Xwing</code> which is a
            hybrid KEM where the share secret outputs of both X25519 and MLKEM768 are combined. </para>
        <para>Finally the <code>ChaChaPoly_BLAKE2b</code> parts of the protocol string indicate
            which stream cipher and hash function we are using. </para>
        <para>As a non-standard modification to the Noise protocol, the 65535 byte message length
            limit is increased to 1300000 bytes. We send very large messages over our Noise protocol
            because of our using the Sphincs+ signature scheme which has signatures that are about
            49k bytes. </para>
        <para>It is assumed that all parties using the KMNWP protocol have a fixed long or short
            lived <code>Xwing</code> keypair <link linkend="XWING">XWING</link>, the public
            component of which is known to the other party in advance. How such keys are distributed
            is beyond the scope of this document. </para>
        <section xml:id="handshake-phase">
            <title>Handshake phase</title>
            <para>All sessions start in the <emphasis>handshake phase</emphasis>, in which an
                anonymous authenticated handshake is conducted. </para>
            <para>The handshake is a unmodified Noise handshake, with a fixed prologue prefacing the
                initiator's first Noise handshake message. This prologue is also used as the
                    <code>prologue</code> input to the Noise HandshakeState
                    <code>Initialize()</code> operation for both the initiator and responder. </para>
            <para>The prologue is defined to be the following structure: </para>
            <programlisting>struct {
uint8_t protocol_version; /* 0x03 */
} Prologue;</programlisting>
            <para>As all Noise handshake messages are fixed sizes, no additional framing is required
                for the handshake. </para>
            <para>Implementations MUST preserve the Noise handshake hash <code>[h]</code> for the
                purpose of implementing authentication (Section 2.3). </para>
            <para>Implementations MUST reject handshake attempts by terminating the session
                immediately upon any Noise protocol handshake failure and when, as a responder, they
                receive a Prologue containing an unknown protocol_version value. </para>
            <para>Implementations SHOULD impose reasonable timeouts for the handshake process, and
                SHOULD terminate sessions that are taking too long to handshake. </para>
        </section>
        <section xml:id="handshake-authentication">
            <title>Handshake authentication</title>
            <para>Mutual authentication is done via exchanging fixed sized payloads as part of the
                    <code>pqXX</code> handshake consisting of the following structure: </para>
            <programlisting>struct {
uint8_t ad_len;
opaque additional_data[ad_len];
opaque padding[255 - ad_len];
uint32_t unix_time;
} AuthenticateMessage;</programlisting>
            <para>Where: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para><code>ad_len</code> - The length of the optional additional data. </para>
                </listitem>
                <listitem>
                    <para><code>additional_data</code> - Optional additional data, such as a
                        username, if any. </para>
                </listitem>
                <listitem>
                    <para><code>unix_time</code> - 0 for the initiator, the approximate number of
                        seconds since 1970-01-01 00:00:00 UTC for the responder. </para>
                </listitem>
            </itemizedlist>
            <para>The initiator MUST send the <code>AuthenticateMessage</code> after it has received
                the peer's response (so after <code>-&gt; s, se</code> in Noise parlance). </para>
            <para>The contents of the optional <code>additional_data</code> field is deliberately
                left up to the implementation, however it is RECOMMENDED that implementations pad
                the field to be a consistent length regardless of contents to avoid leaking
                information about the authenticating identity. </para>
            <para>To authenticate the remote peer given an <code>AuthenticateMessage</code>, the
                receiving peer must validate the <code>s</code> component of the Noise handshake
                (the remote peer's long-term public key) with the known value, along with any of the
                information in the <code>additional_data</code> field such as the user name, if any. </para>
            <para>If the validation procedure succeeds, the peer is considered authenticated. If the
                validation procedure fails for any reason, the session MUST be terminated
                immediately. </para>
            <para>Responders MAY add a slight amount (+- 10 seconds) of random noise to the
                    <code>unix_time</code> value to avoid leaking precise load information via
                packet queueing delay. </para>
        </section>
        <section xml:id="data-transfer-phase">
            <title>Data transfer phase</title>
            <para>Upon successfully concluding the handshake the session enters the <emphasis>data
                    transfer phase</emphasis>, where the initiator and responder can exchange KMNWP
                messages. </para>
            <para>A KMNWP message is defined to be the following structure: </para>
            <programlisting>enum {
no_op(0),
disconnect(1),
send_packet(2),

(255),
} Command;

struct {
Command command;
uint8_t reserved;    /* MUST be '0x00' */
uint32_t msg_length; /* 0 &lt;= msg_length &lt;= 1048554) */
opaque message[msg_length];
opaque padding[];    /* length is implicit */
} Message;</programlisting>
            <para>Notes: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>The padding field, if any MUST be padded with <code>'0x00'</code> bytes. </para>
                </listitem>
            </itemizedlist>
            <para>All outgoing Message(s) are encrypted and authenticated into a pair of Noise
                Transport messages, each containing one of the following structures: </para>
            <programlisting>struct {
uint32_t message_length;
} CiphertextHeader;

struct {
uint32_t message[ciphertext_length-16];
} Ciphertext;</programlisting>
            <para>Notes: </para>
            <itemizedlist spacing="compact">
                <listitem>
                    <para>The <code>ciphertext_length</code> field includes the Noise protocol
                        overhead of 16 bytes, for the Noise Transport message containing the
                        Ciphertext. </para>
                </listitem>
            </itemizedlist>
            <para>All outgoing Message(s) are preceded by a Noise Transport Message containing a
                    <code>CiphertextHeader</code>, indicating the size of the Noise Transport
                Message transporting the Message Ciphertext. After generating both Noise Transport
                Messages, the sender MUST call the Noise CipherState <code>Rekey()</code> operation. </para>
            <para>To receive incoming Ciphertext messages, first the Noise Transport Message
                containing the CiphertextHeader is consumed off the network, authenticated and
                decrypted, giving the receiver the length of the Noise Transport Message containing
                the actual message itself. The second Noise Transport Message is consumed off the
                network, authenticated and decrypted, with the resulting message being returned to
                the caller for processing. After receiving both Noise Transport Messages, the
                receiver MUST call the Noise CipherState <code>Rekey()</code> operation. </para>
            <para>Implementations MUST immediately terminate the session any of the
                    <code>DecryptWithAd()</code> operations fails. </para>
            <para>Implementations MUST immediately terminate the session if an unknown command is
                received in a Message, or if the Message is otherwise malformed in any way. </para>
            <para>Implementations MAY impose a reasonable idle timeout, and terminate the session if
                it expires. </para>
        </section>
    </section>
    <section xml:id="predefined-commands">
        <title>Predefined commands</title>
        <section xml:id="the-no_op-command">
            <title>The <emphasis>no_op</emphasis> command</title>
            <para>The <code>no_op</code> command is a command that explicitly is a No Operation, to
                be used to implement functionality such as keep-alives and or application layer
                padding. </para>
            <para>Implementations MUST NOT send any message payload accompanying this command, and
                all received command data MUST be discarded without interpretation. </para>
        </section>
        <section xml:id="the-disconnect-command">
            <title>The disconnect command</title>
            <para>The disconnect command is a command that is used to signal explicit session
                termination. Upon receiving a disconnect command, implementations MUST interpret the
                command as a signal from the peer that no additional commands will be sent, and
                destroy the cryptographic material in the receive CipherState. </para>
            <para>While most implementations will likely wish to terminate the session upon
                receiving this command, any additional behavior is explicitly left up to the
                implementation and application. </para>
            <para>Implementations MUST NOT send any message payload accompanying this command, and
                MUST not send any further traffic after sending a disconnect command. </para>
        </section>
        <section xml:id="the-send_packet-command">
            <title>The send_packet command</title>
            <para>The <command>send_packet</command> command is the command that is used by the
                initiator to transmit a Sphinx Packet over the network. The command’s message is the
                Sphinx packet destined for the responder. </para>
            <para>Initiators MUST terminate the session immediately upon reception of a
                    <command>send_packet</command> command. </para>
        </section>
    </section>
    <section xml:id="command-padding">
        <title>Command padding</title>
        <para>We use traffic padding to prevent a passive network observer from observing what
            command has been sent or received. The following commands are padded:</para>
        <para><emphasis role="bold">Client-to-server commands</emphasis></para>
        <itemizedlist>
            <listitem>
                <para><command>GetConsensus</command>
                </para>
            </listitem>
            <listitem>
                <para><command>NoOp</command>
                </para>
            </listitem>
            <listitem>
                <para><command>Disconnect</command>
                </para>
            </listitem>
            <listitem>
                <para><command>SendPacket</command>
                </para>
            </listitem>
            <listitem>
                <para><command>RetrieveMessage</command>
                </para>
            </listitem>
        </itemizedlist>        
        <para><emphasis role="bold">Server-to-client commands</emphasis></para>
        <itemizedlist>
            <listitem>
                <para><command>MessageACK</command>
                </para>
            </listitem>
            <listitem>
                <para><command>Message</command>
                </para>
            </listitem>
            <listitem>
                <para><command>MessageEmpty</command>
                </para>
            </listitem>
        </itemizedlist>
        <para>We exclude the <command>Consensus</command> command because its contents are a very
            large payload that is usually many times larger than our Sphinx packets.</para>
        <para>The <command>GetConsensus</command> command is a special case because we only pad it
            when sending it over the mixnet. We don’t pad it when sending to directory authorities,
            because that would needlessly take up bandwidth without providing privacy benefits. </para>
    </section>
    <section xml:id="anonymity-considerations">
        <title>Anonymity considerations</title>
        <para>Adversaries being able to determine that two parties are communicating via KMNWP is
            beyond the threat model of this protocol. At a minimum, it is trivial to determine that
            a KMNWP handshake is being performed, due to the length of each handshake message, and
            the fixed positions of the various public keys. </para>
    </section>
    <section xml:id="security-considerations">
        <title>Security considerations</title>
        <para>It is imperative that implementations use ephemeral keys for every handshake as the
            security properties of the Kyber KEM are totally lost if keys are ever reused. </para>
        <para>Kyber was chosen as the KEM algorithm due to it’s conservative parameterization,
            simplicity of implementation, and high performance in software. It is hoped that the
            addition of a quantum resistant algorithm will provide forward secrecy even in the event
            that large scale quantum computers are applied to historical intercepts. </para>
    </section>
    <section xml:id="acknowledgments">
        <title>Acknowledgments</title>
        <para>I would like to thank Trevor Perrin for providing feedback during the design of this
            protocol, and answering questions regarding Noise. </para>
    </section>
    <section xml:id="appendix-a.-references">
        <title>References</title>        
        &NOISE;
        &NOISEHFS;
        &PQNOISE;
        &RFC2119;
        &RFC5246;
        &RFC7748;        
        &XWING;
    </section>
</section>
