<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="http://docbook.org/xml/5.1/rng/docbook.rng" schematypens="http://relaxng.org/ns/structure/1.0"?>
<?xml-model href="http://docbook.org/xml/5.1/sch/docbook.sch" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>

<!-- The XSL transform inserts these values as Hugo frontmatter. -->
<!-- Additionally, a "date" timestamp is inserted by the stylesheet. -->

<?title "Pigeonhole protocol design" ?> <!-- Required ... copy section title here [https://gohugo.io/content-management/front-matter/#title] -->
<?linkTitle "Pigeonhole Protocol" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#linktitle] -->
<?description "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#description] -->
<?author "Threebit Hacker, David Stainton" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#params] -->
<?url "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#url] -->
<?draft "false" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#draft] -->
<?slug "pigeonhole" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#slug] -->
<?layout "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#layout] -->
<?type "" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#type] -->
<?weight "1" ?> <!-- Optional [https://gohugo.io/content-management/front-matter/#weight] -->
<?version "" ?> <!-- Optional [?] -->

<!DOCTYPE section [
    <!ENTITY % shared-content SYSTEM "/home/dwrob/git/website/source/docbook/shared-content.ent">
        %shared-content;
    <!ENTITY % references SYSTEM "/home/dwrob/git/website/source/docbook/references.ent">
        %references;
    <!ENTITY % terms SYSTEM "/home/dwrob/git/website/source/docbook/terms.ent">
        %terms;    
    <!ELEMENT xi:include (xi:fallback?) > 
    <!ATTLIST xi:include 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" 
    href CDATA #REQUIRED 
    parse (xml|text) "xml" 
    xpointer CDATA #IMPLIED 
    encoding CDATA #IMPLIED 
    accept CDATA #IMPLIED 
    accept-charset CDATA #IMPLIED 
    accept-language CDATA #IMPLIED > 
    <!ELEMENT xi:fallback ANY > 
    <!ATTLIST xi:fallback 
        xmlns:xi CDATA #FIXED "http://www.w3.org/2001/XInclude" > 
    <!ENTITY % local.preface.class "| xi:include"> 
    <!ENTITY % local.part.class "| xi:include"> 
    <!ENTITY % local.chapter.class "| xi:include"> 
    <!ENTITY % local.divcomponent.mix "| xi:include"> 
    <!ENTITY % local.para.char.mix "| xi:include"> 
    <!ENTITY % local.info.class "| xi:include"> 
    <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section    xmlns="http://docbook.org/ns/docbook"
  xmlns:xlink="http://www.w3.org/1999/xlink" version="5.1" 
  xml:id="pigeonhole_protocol">
  <info>  
    <title xml:id="pigeonhole_protocol.title">Pigeonhole protocol design</title> <!-- Match to title PI above. -->
    <authorgroup>
      <author>
        <personname>
          <firstname>Threebit</firstname>
          <surname>Hacker</surname>
        </personname>
      </author>
      <author>
        <personname>
          <firstname>David</firstname>
          <surname>Stainton</surname>
        </personname>
      </author>
    </authorgroup>
  </info>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Abstract</title>
    <para> In this specification we describe the components and protocols that compose Pigeonhole
      scattered storage. We define the behavior of communication clients that send and retrieve
      individual messages, BACAP streams, and AllOrNothing streams. Client actions are mediated
      through courier services that interact with storage replicas. </para>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Introduction</title>
    <para> Pigeonhole scattered storage enables persistent anonymous communication in which
      participants experience a coherent sequence of messages or a continuous data stream, but where
      user relationships and relations between data blocks are unlinkable not only from the
      perspective of third-party observers, but also from that of the mixnet components. This latter
      attribute provides resilience against deanonymization by compromised mixnet nodes. </para>
    <para> The data blocks that Pigeonhole stores are supplied by the BACAP
      (Blinding-and-Capability) scheme. The Pigeonhole protocol scatters messages around many
      storage servers and among a space of BACAP box IDs. From a passive network observer's
      perspective, all of this is seemingly random. All communication among users consists of
      user-generated read or write queries to Pigeonhole storage, never directly to other
      users.</para>
    <para>Many derivative protocols can be composed using Pigeonhole communication channels,
      including group communications. For details about the protocol specified here, see section
      "§5.6. End-to-end reliable group channels" in our paper <link
        xlink:href="https://arxiv.org/abs/2501.02933">Echomix: a Strong Anonymity System with
        Messaging</link>. For more information about BACAP, see §4 of the paper. For an
      understanding of how the core BACAP primitives are implemented, see <link
        xlink:href="https://github.com/katzenpost/hpqc/blob/main/bacap/bacap.go">bacap.go</link>. </para>
    <para>Message-layout snippets in this specification are given in the trunnel binary-format
      description language (matching <code>pigeonhole/pigeonhole_messages.trunnel</code>);
      code snippets showing in-memory courier or replica state are given in Go. </para>
  </section>
  <section xml:id="glossary" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title xml:id="glossary.title">Glossary</title>
    <itemizedlist>
      <listitem>
        <para><emphasis role="strong">Box</emphasis>: BACAP's unit of data storage. Each box has a
          box ID (which also serves as its public key), a signature, and a ciphertext signed
          payload. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Courier</emphasis>: Service that runs on a service node and
          interacts with storage replicas. Proxies requests from clients and routes replies back to
          clients (via SURBs). </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Storage replica</emphasis>: The actual storage nodes where
          message ciphertexts are stored and retrieved. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Intermediate replica</emphasis>: "Intermediate replicas are
          chosen independently of the two final replicas for that box ID which are derived using the
          sharding scheme. The reason Alice designates intermediate replicas, as opposed to
          addressing the final replicas directly, is to avoid revealing to the courier which shard
          the box falls into." (From &quot;§5.4.1. Writing messages&quot; in the <link
            xlink:href="https://arxiv.org/abs/2501.02933">paper</link>.)<emphasis role="strong"
            >Designated replica</emphasis> (a.k.a. <emphasis>final replica</emphasis>,
            <emphasis>shard replica</emphasis>): One of the two replicas selected deterministically
          for a given box ID by the <code>Shard2</code> consistent-hash algorithm. The intermediate
          replicas replicate writes through to the designated replicas. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">EnvelopeHash</emphasis>:
            <code>BLAKE2b-256(CourierEnvelope.sender_pubkey ||
            CourierEnvelope.ciphertext)</code>. Used by the courier for deduplication of
          retransmissions and for demultiplexing replica replies. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">MKEM</emphasis>: Multi-recipient KEM addressed to the pair of
          intermediate replicas. One MKEM ciphertext carries, for each recipient, a separate DEK
          encapsulation (<code>Dek1</code>, <code>Dek2</code>); either recipient may
          decapsulate and recover the padded <code>ReplicaInnerMessage</code>. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Replica-epoch</emphasis>: A one-week period, distinct from the
          20-minute mixnet PKI epoch, during which a given replica-side MKEM envelope keypair is
          valid. See <xref linkend="epochs"/> below. </para>
      </listitem>
    </itemizedlist>
  </section>
  <section xml:id="storage_deployment" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title xml:id="storage_deployment.title">Storage deployment requirements</title>
      <para>A conforming deployment MUST run at least <emphasis role="strong">four</emphasis>
      storage replicas (n ≥ 4). The enables the intermediate-replica unlinkability property
      described in the <xref linkend="glossary"/> and in §5.4.1 of the paper: for each
        <code>CourierEnvelope</code>, the client picks two <emphasis>intermediate</emphasis>
      replicas which MUST be disjoint from the two <emphasis>final</emphasis> (sharded) replicas
      that hold the box. Disjointness is what prevents the courier from learning which shard the box
      falls into. </para>
      <para>A conformant deployment and its undesirable alternatives can be compared as
      follows.</para>
      <itemizedlist spacing="compact">
        <listitem>
        <para><emphasis role="strong">n ≥ 4</emphasis>: At least two non-shard replicas exist, so
          the intermediate set can be drawn uniformly at random from the non-shard subset, and
          disjointness is preserved.</para>
        </listitem>
      <listitem>
        <para><emphasis role="strong">n = 3</emphasis>: Only one replica sits outside any given
          two-shard set, so the client cannot pick two independent non-shard intermediates.
          Implementations in this case fall back to an intermediate set that includes at least one
          shard replica, which tells the courier that replica is one of the two shards for the box. </para>
        <para>The resulting information exposure is bounded:</para>
        <para>
          <itemizedlist>
            <listitem>
              <para>The courier does NOT learn the box ID. The box ID lives inside the
                MKEM-encrypted inner message addressed to the intermediate replicas; only the
                replicas can decrypt it. </para>
            </listitem>
            <listitem>
              <para>The courier does NOT learn the other shard member. Knowing one element of the
                two-element shard set does not constrain the identity of the other among the
                remaining replicas. </para>
            </listitem>
            <listitem>
              <para>The courier does NOT gain a way to link boxes in the same sequence. BACAP's
                unlinkability of consecutive box IDs (paper §4) is a property of the
                capability-derivation scheme and is independent of sharding. </para>
            </listitem>
          </itemizedlist>
        </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">n = 2</emphasis>: There is only one possible shard set, and
          the intermediate set is necessarily identical to it.</para>
      </listitem>
      </itemizedlist>
    <para>Consequently, the main unlinkability properties of Pigeonhole are not lost at <emphasis>n
        = 3</emphasis>, but the defense-in-depth margin provided by disjoint intermediate replicas
      is. Operators SHOULD treat <emphasis>n ≥ 4</emphasis> as the minimum supported configuration. </para>
    </section>
  <section xml:id="epochs" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title xml:id="epochs.title">Epochs</title>
    <para>Katzenpost has two distinct notions of &quot;epoch&quot; that operate on very different
      timescales, and Pigeonhole touches both: </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para><emphasis role="strong">Mixnet epoch</emphasis> (a.k.a. <emphasis>normal
            epoch</emphasis>, <emphasis>PKI epoch</emphasis>): the short cadence on which the
          directory authorities publish a new PKI document. The default is 20 minutes. Mix nodes,
          gateways, service nodes, and clients all synchronise on this epoch. Sphinx replay keys on
          mix nodes rotate once per mixnet epoch. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Pigeonhole storage-replica epoch</emphasis>: the long cadence
          on which each storage replica rotates its MKEM envelope keypair. The default is one week.
          A replica publishes, in every mixnet-epoch PKI descriptor, <emphasis>two</emphasis>
          envelope public keys: the current replica-epoch key and the next replica-epoch key. </para>
      </listitem>
    </itemizedlist>
    <section>
      <title>Epoch tolerance for CourierEnvelope</title>
      <para>A <code>CourierEnvelope</code> carries an <code>epoch</code> field that identifies the
        replica epoch whose envelope key the client used to encrypt the MKEM ciphertext. Conforming
        couriers and storage replicas MUST accept <code>epoch ∈ {current − 1, current, current +
          1}</code> where <code>current</code> is the courier's / replica's own view of the current
        replica epoch. </para>
      <para>The <code>current − 1</code> tolerance handles the grace window immediately after
        a replica-epoch boundary, when a client with a slightly stale PKI view still encrypts to the
        previous envelope public key. Combined with <code>current</code>, this gives a
          <emphasis role="strong">two replica-epoch data TTL</emphasis> — roughly two weeks —
        because the future replica-epoch key is by definition a key that hasn't started being used
        yet, so <code>current</code> and <code>current − 1</code> are the epochs where
        actual data flows. </para>
      <para>The <code>current + 1</code> tolerance handles the same boundary seen from the
        other side: a client whose clock or PKI view is slightly <emphasis>ahead</emphasis> of the
        courier / replica. </para>
      <para>Envelopes outside this three-epoch window MUST be rejected, because by definition no
        replica still holds the matching decapsulation key: </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>Older than <code>current − 1</code>: the envelope public key has been pruned
            from replicas (see the replica's envelope-key GC worker). The replica cannot decrypt and
            the courier can fail fast rather than forward a doomed request. </para>
        </listitem>
        <listitem>
          <para>Newer than <code>current + 1</code>: no replica has generated that envelope
            key yet (replicas generate <code>current</code> and <code>current + 1</code>
            only). </para>
        </listitem>
      </itemizedlist>
      <para>Couriers SHOULD reject with a dedicated envelope-level error code
          (<code>EnvelopeErrorInvalidEpoch</code>) so clients can distinguish &quot;stale
        encryption&quot; from other courier-side rejections. </para>
    </section>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Pigeonhole message format and constants</title>
    <para>The Pigeonhole message types are defined in trunnel at <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/pigeonhole/pigeonhole_messages.trunnel"
          ><code>pigeonhole/pigeonhole_messages.trunnel</code></link>; the Go bindings live in <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/pigeonhole/trunnel_messages.go"
          ><code>pigeonhole/trunnel_messages.go</code></link>. All integer fields are big-endian and
      all variable-length fields carry an explicit length prefix. This trunnel encoding replaces the
      earlier CBOR encoding with a fixed-overhead binary format whose serialised size can be
      computed deterministically from the Sphinx geometry. </para>
    <para>Carriage of these messages differs by hop: </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para><emphasis role="strong">Client → Courier:</emphasis> a <code>CourierQuery</code> is
          carried inside a Sphinx packet payload. (See <xref linkend="courier_query"/> for details.)
          The reverse direction uses a SURB supplied by the client. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">Courier → Replica and Replica → Replica:</emphasis> the
          courier and replicas do not use Sphinx packets between themselves. They communicate over
          the Katzenpost wire protocol defined in <code>core/wire</code> and
            <code>core/wire/commands</code>; the relevant commands are <code>ReplicaMessage</code>,
            <code>ReplicaMessageReply</code>, <code>ReplicaWrite</code>, and
            <code>ReplicaWriteReply</code>. Some of these commands embed trunnel-serialised
          pigeonhole blobs as their payload. </para>
      </listitem>
    </itemizedlist>
    <section>
      <title>Fundamental size constants</title>
      <table frame="all">
        <info><title></title></info>
        <tgroup cols="3">
          <colspec colnum="1" colname="c1" colwidth="1*"/>
          <colspec colnum="2" colname="c2" colwidth="1*"/>
          <colspec colnum="3" colname="c3" colwidth="1*"/>
          <thead>
            <row>
              <entry>Constant</entry>
              <entry>Value</entry>
              <entry>Source </entry>
            </row>
          </thead>
          <tbody>
            <row>
              <entry><code>BACAP_BOX_ID_SIZE</code></entry>
              <entry>32 bytes</entry>
              <entry>Ed25519 public key</entry>
            </row>
            <row>
              <entry><code>BACAP_SIGNATURE_SIZE</code></entry>
              <entry>64 bytes</entry>
              <entry>Ed25519 signature</entry>
            </row>
            <row>
              <entry><code>HASH_SIZE</code></entry>
              <entry>32 bytes</entry>
              <entry>BLAKE2b-256</entry>
            </row>
            <row>
              <entry><code>MKEM_DEK_SIZE</code></entry>
              <entry>60 bytes</entry>
              <entry>mkem.DEKSize</entry>
            </row>
            <row>
              <entry>CTIDH-1024 public key</entry>
              <entry>160 bytes</entry>
              <entry>hpqc/nike/ctidh/ctidh1024</entry>
            </row>
            <row>
              <entry>X25519 public key</entry>
              <entry>32 bytes</entry>
              <entry><code>hpqc/nike/x25519</code></entry>
            </row>
            <row>
              <entry>Hybrid CTIDH-1024 × X25519 NIKE public key</entry>
              <entry>192 bytes</entry>
              <entry>sum of the above</entry>
            </row>
            <row>
              <entry>BACAP payload encryption overhead</entry>
              <entry>16 bytes</entry>
              <entry>ChaCha20-Poly1305 AEAD</entry>
            </row>
            <row>
              <entry>MKEM encapsulation overhead</entry>
              <entry>28 bytes</entry>
              <entry>ChaCha20-Poly1305 nonce (12) + tag (16)</entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </section>
    <section>
      <title>Maximum BACAP payload</title>
      <para>The maximum plaintext BACAP payload a single box can carry is derived
          <emphasis>backwards</emphasis> from the Sphinx <code>UserForwardPayloadLength</code> by
        subtracting every layer of framing and cryptographic overhead that sits between the Sphinx
        payload and the BACAP plaintext. The authoritative calculation is performed by
          <code>NewGeometryFromSphinx</code> in <link
          xlink:href="https://github.com/katzenpost/katzenpost/blob/main/pigeonhole/geo/geometry.go"
            ><code>pigeonhole/geo/geometry.go</code></link>. </para>
      <para>Informally: </para>
      <programlisting>MaxPlaintextPayloadLength
  = UserForwardPayloadLength
    − CourierQuery framing (1 byte: query_type discriminator)
    − CourierEnvelope header (intermediate_replicas[2] + Dek1 + Dek2
                              + reply_index + epoch + sender_pubkey_len
                              + sender_pubkey + ciphertext_len)
    − MKEM AEAD overhead (28 bytes)
    − ReplicaInnerMessage framing (1 byte: message_type discriminator)
    − ReplicaWrite header (box_id + signature + payload_len = 100 bytes)
    − BACAP AEAD overhead (16 bytes)
    − BACAP length prefix (4 bytes)  </programlisting>
      <para>With a hybrid CTIDH-1024 × X25519 NIKE, the fixed per-packet overhead between
          <code>UserForwardPayloadLength</code> and the BACAP plaintext is on the order of 560
        bytes; the exact figure depends on the configured NIKE scheme and should always be obtained
        from <code>geometry.NewGeometryFromSphinx()</code> rather than computed by hand. </para>
    </section>
    <section xml:id="courier_envelope" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
      <title xml:id="courier_envelope.title">The CourierEnvelope as seen by the courier</title>
      <para>The courier, upon unwrapping the Sphinx payload of a client's packet, sees a
          <code>CourierQuery</code> whose <code>content</code> is a <code>CourierEnvelope</code>
        with the following layout: </para>
      <programlisting>struct courier_envelope {
    u8  intermediate_replicas[2];          // replica indices in the PKI
    u8  dek1[MKEM_DEK_SIZE];               // DEK encapsulation for replica 0
    u8  dek2[MKEM_DEK_SIZE];               // DEK encapsulation for replica 1
    u8  reply_index;                       // which replica's reply to prefer
    u64 epoch;                             // replica-epoch under which the
                                           //   MKEM ciphertext was produced
    u16 sender_pubkey_len;
    u8  sender_pubkey[sender_pubkey_len];  // client's ephemeral hybrid NIKE pk
    u32 ciphertext_len;
    u8  ciphertext[ciphertext_len];        // MKEM-encrypted ReplicaInnerMessage
} </programlisting>
      <para>Notable points: </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>The <code>ciphertext</code> is opaque to the courier. It is an MKEM envelope
            addressed to the <emphasis>pair</emphasis> of intermediate replicas; either replica can
            decapsulate using its own DEK (<code>dek1</code> or <code>dek2</code> respectively). </para>
        </listitem>
        <listitem>
          <para>The <code>epoch</code> field names the <emphasis>replica-epoch</emphasis> whose
            envelope keys were used to produce the MKEM ciphertext. See <xref linkend="epochs"/> for
            the tolerance window. </para>
        </listitem>
        <listitem>
          <para>Prior to encryption, the inner <code>ReplicaInnerMessage</code> is zero-padded to
              <code>ReplicaInnerMessageWriteSize()</code> so that reads, writes and tombstones
            produce MKEM ciphertexts of identical length. </para>
        </listitem>
      </itemizedlist>
    </section>
    <section>
      <title>The ReplicaInnerMessage as seen by a replica</title>
      <para>Once an intermediate replica decrypts the MKEM envelope, it obtains a
          <code>ReplicaInnerMessage</code> — a discriminated union over <code>message_type</code>: </para>
      <programlisting>struct replica_inner_message {
    u8 message_type IN [0, 1];    // 0 = read, 1 = write
    union content[message_type] {
        0: struct replica_read  read_msg;   // { box_id[32] }
        1: struct replica_write write_msg;  // { box_id[32], signature[64],
                                            //   payload_len[u32], payload[] }
    };
} </programlisting>
      <para>A <code>ReplicaWrite</code> with <code>payload_len == 0</code> is a <link
          linkend="tombstones">tombstone</link>. </para>
    </section>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Message types and interactions</title>
    <section>
      <title>Overview</title>
      <itemizedlist spacing="compact">
        <listitem>
          <para>A client sends a <code>CourierQuery</code> inside a Sphinx packet payload. The
            courier's reply travels back to the client by means of a SURB the client also supplied. </para>
        </listitem>
        <listitem>
          <para>A client always designates <emphasis role="strong">two</emphasis> intermediate
            replicas per <code>CourierEnvelope</code>. The courier dispatches the corresponding pair
            of <code>ReplicaMessage</code> wire commands, one to each intermediate, and collects up
            to two <code>ReplicaMessageReply</code> results. </para>
        </listitem>
        <listitem>
          <para>The <code>reply_index</code> field is a <emphasis>preference</emphasis> indicating
            which of the two replica replies the client would like forwarded first. It is not a
            strict selector: should the preferred slot still be empty when the courier is ready to
            respond, the courier falls back to whichever reply it does hold, and indicates in the
              <code>CourierEnvelopeReply</code> the actual index that was served (see <link
              xlink:href="https://github.com/katzenpost/katzenpost/blob/main/courier/server/plugin.go"
                ><code>courier/server/plugin.go</code></link>). </para>
        </listitem>
        <listitem>
          <para>Clients MUST resend identical <code>CourierEnvelope</code> bodies — same
              <code>sender_pubkey</code> and <code>ciphertext</code> — until they receive a reply.
            The courier deduplicates resends by <code>EnvelopeHash</code>. Only the Sphinx-layer
            SURB is rotated between retransmissions. </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="courier_query" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
      <title xml:id="courier_query.title">CourierQuery</title>
      <para>A <code>CourierQuery</code> is the top-level discriminated union that a client places
        into a Sphinx packet payload for the courier: </para>
      <programlisting>struct courier_query {
    u8 query_type IN [0, 1];
    union content[query_type] {
        0: struct courier_envelope envelope;      // read or write a single box
        1: struct copy_command     copy_command;  // AllOrNothing copy
    };
}  </programlisting>
      <para>The symmetric reply is a <code>CourierQueryReply</code>: </para>
      <programlisting>struct courier_query_reply {
    u8 reply_type IN [0, 1];
    union content[reply_type] {
        0: struct courier_envelope_reply envelope_reply;
        1: struct copy_command_reply     copy_command_reply;
    };
}  </programlisting>
    </section>
    <section>
      <title>CourierEnvelope</title>
      <para>The <code>CourierEnvelope</code> layout was given in <xref linkend="courier_envelope"/>.
        The client constructs one <code>CourierEnvelope</code> per read or write. The courier
        performs the following actions.</para>
      <orderedlist numeration="arabic" spacing="compact">
        <listitem>
          <para>Computes <code>EnvelopeHash = BLAKE2b-256(sender_pubkey || ciphertext)</code>. </para>
        </listitem>
        <listitem>
          <para>Verifies <code>epoch ∈ {current − 1, current, current + 1}</code> per the
            replica-epoch tolerance window, otherwise rejecting with
              <code>EnvelopeErrorInvalidEpoch</code>. </para>
        </listitem>
        <listitem>
          <para>Looks <code>EnvelopeHash</code> up in its dedup cache. If present, the courier
            returns the cached reply (or an <code>ACK</code> if no reply has yet arrived) without
            re-dispatching to replicas. </para>
        </listitem>
        <listitem>
          <para>On a cache miss, the courier constructs two <code>ReplicaMessage</code> wire
            commands — one bound for <code>intermediate_replicas[0]</code> carrying
              <code>dek1</code>, the other for <code>intermediate_replicas[1]</code> carrying
              <code>dek2</code> — and forwards them over the wire protocol. </para>
        </listitem>
        <listitem>
          <para>The courier immediately sends an <code>ACK</code> reply to the client so it may stop
            retransmitting. </para>
        </listitem>
      </orderedlist>
    </section>
    <section>
      <title>ReplicaMessage (wire command)</title>
      <para><code>ReplicaMessage</code> is <emphasis>not</emphasis> a trunnel pigeonhole type; it is
        the <code>core/wire/commands</code> command sent from a courier to a replica. Its payload
        fields are copied verbatim from the matching <code>CourierEnvelope</code>: </para>
      <programlisting language="go">// core/wire/commands
  pe ReplicaMessage struct {
    SenderEPubKey []byte     // copied from CourierEnvelope.sender_pubkey
    DEK           *[MKEM_DEK_SIZE]byte  // dek1 or dek2, depending on destination
    Ciphertext    []byte     // copied from CourierEnvelope.ciphertext
}</programlisting>
      <para>The recipient replica decapsulates the MKEM envelope using the replica-epoch key that
        corresponds to <code>CourierEnvelope.epoch</code> (trying each of the three keys in its
        tolerance window), yielding a padded <code>ReplicaInnerMessage</code>. The replica then
        dispatches on <code>message_type</code> to <code>ReplicaRead</code> or
          <code>ReplicaWrite</code> handling. </para>
    </section>
    <section>
      <title>ReplicaMessageReply (wire command)</title>
      <para>In response to a <code>ReplicaMessage</code>, the courier expects an asynchronous
          <code>ReplicaMessageReply</code> wire command from the replica: </para>
      <programlisting language="go">// core/wire/commands
type ReplicaMessageReply struct {
    ErrorCode     uint8                 // see the replica error-code table below
    EnvelopeHash  *[HASH_SIZE]byte      // lets the courier demultiplex the reply
    EnvelopeReply []byte                // MKEM-encrypted ReplicaMessageReplyInnerMessage
}</programlisting>
      <para>The <code>EnvelopeReply</code> byte blob is produced by the replica via the MKEM
        scheme's <code>EnvelopeReply()</code> method (see <link
          xlink:href="https://github.com/katzenpost/katzenpost/blob/main/replica/handlers.go"
            ><code>replica/handlers.go</code></link>). It carries a
          <code>ReplicaMessageReplyInnerMessage</code> — a discriminated union over either a
          <code>ReplicaReadReply</code> or a <code>ReplicaWriteReply</code> — padded to
          <code>ReplicaReplyInnerMessageReadSize()</code> so that read replies and write replies are
        indistinguishable in size, and encrypted to the client's ephemeral NIKE public key under the
        replica's envelope keypair. </para>
    </section>
    <section>
      <title>CourierBookKeeping</title>
      <para>For each outstanding <code>EnvelopeHash</code>, the courier maintains an in-memory dedup
        entry. Its actual structure is: </para>
      <programlisting language="go">// courier/server/plugin.go
type CourierBookKeeping struct {
    Epoch                uint64        // replica-epoch at cache insertion
    CreatedAt            time.Time     // for TTL eviction (~5 minutes)
    QueryType            uint8         // the query_type that produced this entry
    IntermediateReplicas [2]uint8
    EnvelopeReplies      [2]*commands.ReplicaMessageReply
}  </programlisting>
      <para>Note that the courier does <emphasis role="strong">not</emphasis> cache SURBs or SURB
        timestamps. A client's SURB is consumed by the Sphinx-layer plugin infrastructure at the
        moment the courier emits a reply and is not retained by the courier's Pigeonhole state.
        Should the client not receive that reply, it is expected to retransmit a fresh Sphinx packet
        carrying a fresh SURB but the identical <code>CourierEnvelope</code> body; the courier,
        recognising the <code>EnvelopeHash</code>, replies using the new SURB. </para>
      <para>The dedup cache has a TTL of 5 minutes (<code>DedupCacheTTL</code> in <link
          xlink:href="https://github.com/katzenpost/katzenpost/blob/main/courier/server/plugin.go"
            ><code>courier/server/plugin.go</code></link>). </para>
    </section>
    <section>
      <title>CourierEnvelopeReply</title>
      <para>The courier's reply to a <code>CourierEnvelope</code> has the following trunnel layout: </para>
      <programlisting>struct courier_envelope_reply {
    u8  envelope_hash[HASH_SIZE];       // identifies the originating envelope
    u8  reply_index;                    // which intermediate replica's reply
                                        //   is being returned (may differ
                                        //   from the client's requested index)
    u8  reply_type IN [0, 1];           // 0 = ACK, 1 = PAYLOAD
    u32 payload_len;
    u8  payload[payload_len];           // the MKEM-encrypted EnvelopeReply,
                                        //   present iff reply_type == PAYLOAD
    u8  error_code;                     // see envelope error codes below
}</programlisting>
      <para>A <code>reply_type</code> of <code>ACK</code> (0) indicates the courier has received the
        envelope and dispatched it to the replicas but has not yet received a reply for the
        requested index. A <code>reply_type</code> of <code>PAYLOAD</code> (1) indicates the
          <code>payload</code> field carries the MKEM-encrypted <code>EnvelopeReply</code> produced
        by a replica. </para>
    </section>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Embedded pigeonhole types</title>
    <para>These trunnel structs are not carried on the wire in isolation; they are embedded inside
      MKEM envelopes and their replies. </para>
    <section>
      <title>ReplicaRead</title>
      <para>Embedded inside the MKEM-encrypted <code>ReplicaInnerMessage</code> a client sends to a
        replica (via the courier) for a read operation. </para>
      <programlisting>struct replica_read {
    u8 box_id[BACAP_BOX_ID_SIZE];
}  </programlisting>
    </section>
    <section>
      <title>ReplicaReadReply</title>
      <para>Embedded inside the MKEM-encrypted <code>ReplicaMessageReplyInnerMessage</code> a
        replica returns for a successful read. Padding is applied at the outer
          <code>ReplicaMessageReplyInnerMessage</code> level; this struct carries no padding of its
        own. </para>
      <programlisting>struct replica_read_reply {
    u8  error_code;
    u8  box_id[BACAP_BOX_ID_SIZE];
    u8  signature[BACAP_SIGNATURE_SIZE];
    u32 payload_len;
    u8  payload[payload_len];
}</programlisting>
    </section>
    <section>
      <title>ReplicaWrite</title>
      <para>Used both (a) embedded inside the MKEM-encrypted <code>ReplicaInnerMessage</code> for a
        client write, and (b) carried directly as a <code>core/wire/commands</code> command between
        replicas during replication. </para>
      <programlisting>struct replica_write {
    u8  box_id[BACAP_BOX_ID_SIZE];
    u8  signature[BACAP_SIGNATURE_SIZE];
    u32 payload_len;
    u8  payload[payload_len];
}</programlisting>
      <para>A <code>ReplicaWrite</code> with <code>payload_len == 0</code> is a <emphasis
          role="strong">tombstone</emphasis>. Replicas treat tombstone writes as overwrites: an
        existing box at the same <code>box_id</code> is replaced by the tombstone, and subsequent
        reads return <code>ReplicaErrorTombstone</code>. </para>
    </section>
    <section>
      <title>ReplicaWriteReply</title>
      <para>
        Embedded inside a
        <code>ReplicaMessageReplyInnerMessage</code> on the client
        reply path, and also used as the
        <code>core/wire/commands</code> reply to inter-replica
        replication.
      </para>
      <programlisting>struct replica_write_reply {
    u8 error_code;
}</programlisting>
    </section>
  </section>
  
  
  <section xml:id="tombstones" xmlns="http://docbook.org/ns/docbook"
    xmlns:xlink="http://www.w3.org/1999/xlink">
    <title xml:id="tombatines">Tombstones</title>
    <para>A <code>ReplicaWrite</code> whose <code>payload_len == 0</code> is a
        <emphasis>tombstone</emphasis>. It marks a box as deleted without revealing that fact to the
      courier.</para>
    <itemizedlist>
      <listitem>
        <para>Replicas treat a tombstone write as an ordinary overwrite. An existing
            <code>Box</code> at the same <code>box_id</code> is replaced by the tombstone, and
          subsequent reads of that <code>box_id</code> return <code>ReplicaErrorTombstone</code>.
          (See <xref linkend="replica_error_codes"/>.)</para>
      </listitem>
      <listitem>
        <para><code>ReplicaErrorTombstone</code> is an <emphasis role="italic">expected</emphasis>
          outcome rather than a failure; it positively confirms that the box was deleted, as
          distinct from <code>ReplicaErrorBoxNotFound</code>.</para>
      </listitem>
      <listitem>
        <para>Because the inner <code>ReplicaInnerMessage</code> is zero-padded to
            <code>ReplicaInnerMessageWriteSize()</code> before MKEM encryption (as described in
            <xref linkend="courier_envelope"/>), a tombstone write produces an MKEM ciphertext of
          exactly the same length as a non-empty write or a read. A passive observer therefore
          cannot distinguish deletion from any other operation.</para>
      </listitem>
    </itemizedlist>
    <para>Tombstones are also used by the <link linkend="allornothing">AllOrNothing copy
        protocol</link>. After processing a <code>CopyCommand</code>, the courier overwrites every
      box of the temporary stream with tombstones.</para>
  </section>
  
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>EnvelopeHash</title>
    <para>
      The <code>EnvelopeHash</code> uniquely identifies a
      <code>CourierEnvelope</code> for the purposes of deduplication
      and reply demultiplexing. It is computed as:
    </para>
    <programlisting>EnvelopeHash = BLAKE2b-256(sender_pubkey || ciphertext)</programlisting>
    <para>where <code>sender_pubkey</code> and <code>ciphertext</code> are the corresponding fields
      of the <code>CourierEnvelope</code>. The implementation is <code>CourierEnvelope.EnvelopeHash()</code> in <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/pigeonhole/helpers.go"
          ><code>pigeonhole/helpers.go</code></link>. </para>
    <para>A retransmitted <code>CourierEnvelope</code> carries the same <code>sender_pubkey</code>
      and <code>ciphertext</code> as the original, and therefore hashes to the same value; only the
      surrounding Sphinx packet (and its SURB) changes between attempts. </para>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Error codes</title>
    <para>All error codes are defined in <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/pigeonhole/errors.go"
          ><code>pigeonhole/errors.go</code></link>. </para>
    <section xml:id="replica_error_codes" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
      <title xml:id="replica_error_codes.title">Replica error codes</title>
      <para>Returned by a replica in <code>ReplicaMessageReply.ErrorCode</code>,
          <code>ReplicaReadReply.error_code</code>, and <code>ReplicaWriteReply.error_code</code>. </para>
      <table frame="all">
        <title/>
        <tgroup cols="3">
          <colspec colnum="1" colname="c1" colwidth="1*"/>
          <colspec colnum="2" colname="c2" colwidth="1*"/>
          <colspec colnum="3" colname="c3" colwidth="1*"/>
          <thead>
            <row>
              <entry>Code</entry>
              <entry>Name</entry>
              <entry>Meaning</entry>
            </row>
          </thead>
          <tbody>
            <row>
              <entry>0</entry>
              <entry><code>ReplicaSuccess</code></entry>
              <entry>Operation completed successfully</entry>
            </row>
            <row>
              <entry>1</entry>
              <entry><code>ReplicaErrorBoxIDNotFound</code></entry>
              <entry>Read miss (expected outcome)</entry>
            </row>
            <row>
              <entry>2</entry>
              <entry><code>ReplicaErrorInvalidBoxID</code></entry>
              <entry>Malformed box ID</entry>
            </row>
            <row>
              <entry>3</entry>
              <entry><code>ReplicaErrorInvalidSignature</code></entry>
              <entry>BACAP signature verification failed</entry>
            </row>
            <row>
              <entry>4</entry>
              <entry><code>ReplicaErrorDatabaseFailure</code></entry>
              <entry>Transient RocksDB error</entry>
            </row>
            <row>
              <entry>5</entry>
              <entry><code>ReplicaErrorInvalidPayload</code></entry>
              <entry>Malformed payload</entry>
            </row>
            <row>
              <entry>6</entry>
              <entry><code>ReplicaErrorStorageFull</code></entry>
              <entry>Storage capacity exceeded</entry>
            </row>
            <row>
              <entry>7</entry>
              <entry><code>ReplicaErrorInternalError</code></entry>
              <entry>Internal server error</entry>
            </row>
            <row>
              <entry>8</entry>
              <entry><code>ReplicaErrorInvalidEpoch</code></entry>
              <entry>Replica-epoch envelope key unavailable</entry>
            </row>
            <row>
              <entry>9</entry>
              <entry><code>ReplicaErrorReplicationFailed</code></entry>
              <entry>Replication to shard peer failed</entry>
            </row>
            <row>
              <entry>10</entry>
              <entry><code>ReplicaErrorBoxAlreadyExists</code></entry>
              <entry>Idempotent-write outcome (expected)</entry>
            </row>
            <row>
              <entry>11</entry>
              <entry><code>ReplicaErrorTombstone</code></entry>
              <entry>Read returned a tombstone (expected)</entry>
            </row>
          </tbody>
        </tgroup>
      </table>
      <para>Codes 1, 10 and 11 are &quot;expected outcomes&quot; — they correspond to normal
        protocol states rather than faults. The thin-client helper
          <code>thin.IsExpectedOutcome(err)</code> treats these three codes as non-errors. </para>
    </section>
    <section>
      <title>Courier envelope error codes</title>
      <para> Returned by the courier in <code>CourierEnvelopeReply.error_code</code>. </para>
      <table frame="all">
        <title/>
        <tgroup cols="3">
          <colspec colnum="1" colname="c1" colwidth="1*"/>
          <colspec colnum="2" colname="c2" colwidth="1*"/>
          <colspec colnum="3" colname="c3" colwidth="1*"/>
          <thead>
            <row>
              <entry>Code</entry>
              <entry>Name</entry>
              <entry>Meaning</entry>
            </row>
          </thead>
          <tbody>
            <row>
              <entry>0</entry>
              <entry><code>EnvelopeErrorSuccess</code></entry>
              <entry>Operation completed.</entry>
            </row>
            <row>
              <entry>1</entry>
              <entry><code>EnvelopeErrorInvalidEnvelope</code></entry>
              <entry>Malformed envelope (e.g. <code>reply_index &gt; 1</code>) </entry>
            </row>
            <row>
              <entry>2</entry>
              <entry><code>EnvelopeErrorCacheCorruption</code>
              </entry>
              <entry>Internal cache inconsistency</entry>
            </row>
            <row>
              <entry>3</entry>
              <entry>
                <code>EnvelopeErrorPropagationError</code></entry>
              <entry>Failed to dispatch to replicas.</entry>
            </row>
            <row>
              <entry>4</entry>
              <entry><code>EnvelopeErrorInvalidEpoch</code></entry>
              <entry>
                <code>CourierEnvelope.epoch</code> outside tolerance window.</entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </section>
    <section>
      <title>Copy command status codes</title>
      <para> Returned by the courier in <code>CopyCommandReply.status</code> (see below). </para>
      <para>Returned by the courier in the <code>status</code> field of a
          <code>CopyCommandReply</code>. (See <xref linkend="copycommandreply"/> for more
        information.)</para>
      <table frame="all">
        <info>
          <title/>
        </info>
        <tgroup cols="3">
          <colspec colnum="1" colname="c1" colwidth="1*"/>
          <colspec colnum="2" colname="c2" colwidth="1*"/>
          <colspec colnum="3" colname="c3" colwidth="1*"/>
          <thead>
            <row>
              <entry>Code</entry>
              <entry>Name</entry>
              <entry>Meaning</entry>
            </row>
          </thead>
          <tbody>
            <row>
              <entry>0</entry>
              <entry>
                <code>CopyStatusSucceeded</code></entry>
              <entry>All destination writes completed.</entry>
            </row>
            <row>
              <entry>1</entry>
              <entry><code>CopyStatusInProgress</code></entry>
              <entry>Courier has accepted the command; processing continues.</entry>
            </row>
            <row>
              <entry>2</entry>
              <entry><code>CopyStatusFailed</code></entry>
              <entry>Aborted; see <code>error_code</code> +
                <code>failed_envelope_index</code>.</entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </section>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Sharding and replica selection</title>
    <para>For each box, two <emphasis>designated</emphasis> (final) replicas are derived
      deterministically from the box ID using the <code>Shard2</code> consistent-hash algorithm.
      (See <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/replica/common/shard.go"
          ><code>replica/common/shard.go</code></link>.): </para>
    <programlisting>for each online replica r with identity key k_r:
    h_r = BLAKE2b-256(k_r || box_id)
return the two replicas whose h_r are smallest</programlisting>
    <para>The two <emphasis>intermediate</emphasis> replicas chosen by the client for a given
        <code>CourierEnvelope</code> are drawn independently of the designated replicas, per
        <code>pigeonhole.GetRandomIntermediateReplicas</code>: </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para><emphasis role="strong">n ≥ 4 replicas:</emphasis> two intermediates are drawn
          uniformly at random from the replicas that are <emphasis role="strong">not</emphasis> in
          the designated (shard) set — preserving the disjointness invariant described in <xref
            linkend="storage_deployment"/>.
          <?oxy_custom_start type="oxy_content_highlight" color="255,255,0"?></para>
      </listitem><?oxy_custom_end?>
      <listitem>
        <para><emphasis role="strong">n = 3:</emphasis> fallback — at least one intermediate must
          coincide with a designated replica; this weakens the intermediate/final disjointness
          guarantee but does not compromise box unlinkability. </para>
      </listitem>
      <listitem>
        <para><emphasis role="strong">n &lt; 3:</emphasis> rejected. </para>
      </listitem>
    </itemizedlist>
    <para>Intermediate replicas, upon accepting a <code>ReplicaWrite</code>, compute the designated
      replicas themselves and forward the <code>ReplicaWrite</code> to them via the
        <code>core/wire/commands</code>
      <code>ReplicaWrite</code> command (see <link
        xlink:href="https://github.com/katzenpost/katzenpost/blob/main/replica/connector.go"
          ><code>replica/connector.go</code></link>). </para>
  </section>
  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Protocol sequence visualizations</title>
    <para>For simplicity, the following diagrams omit replication while illustrating the Pigeonhole
      write and read operations. </para>
    <para><emphasis role="strong">Pigeonhole write operation</emphasis>
    </para>
    <para>      
      <figure xmlns:id="1">
        <title>Annotated writes</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="pix/annotated_write.png" />
          </imageobject>                  
          <textobject>
          <phrase>Protocol annotatation image of writes.</phrase>
        </textobject>
        </mediaobject>
      </figure>
     </para>
    <para>
      <emphasis role="strong">Pigeonhole read operation</emphasis>
    </para>
    <para>
      <figure xmlns:id="2">
        <title>Annotated reads</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="pix/annotated_read.png" />
          </imageobject>                  
          <textobject>
          <phrase>Protocol annotatation image of reads.</phrase>
        </textobject>
        </mediaobject>
      </figure>   
    </para>
  </section>
  <section xml:id="allornothing" xmlns="http://docbook.org/ns/docbook"
    xmlns:xlink="http://www.w3.org/1999/xlink">
    <title xml:id="allornothing.title">Pigeonhole AllOrNothing protocol</title>
    <para>The AllOrNothing delivery mechanism ensures that a set of associated BACAP writes either
      succeeds or fails atomically from the point of view of a replica or second-party client
      reader. This behavior prevents an adversary from detecting a correlation between (A) the
      sending client's failure to transmit multiple messages at once with (B) a network interruption
      on the sending client's side of the network. Regardless of the number of messages in the set,
      the adversary gets to observe &quot;at most once&quot; that the sending client interacted with
      the network. </para>
    <para>The protocol works as follows. </para>
    <section>
      <title>Step 1</title>
      <para>The client uploads a <emphasis>temporary Pigeonhole stream</emphasis>. </para>
      <para>The stream conveys a sequence of <code>CourierEnvelope</code>s. Each
          <code>CourierEnvelope</code> is serialised and prefixed with a single 4-byte
          (<code>u32</code>) length field giving the size of that one envelope; the resulting
        length-prefixed blobs are concatenated back-to-back into one continuous byte stream.</para>
      <para>That byte stream is then split across the BACAP Boxes of the temporary stream. A
        serialised <code>CourierEnvelope</code> is strictly larger than the maximum BACAP box
        payload (it wraps a full box payload plus its own metadata), so a single envelope does not
        fit in one box and the concatenated stream necessarily spans several boxes. Envelope
        boundaries therefore do not align with box boundaries. The precise framing is given in <xref
          linkend="temp_stream"/>.</para>
    </section>
    <section>
      <title>Step 2</title>
      <para>The client sends a random courier the copy command which encapsulates the write
        capability to the temporary Pigeonhole stream written in Step 1 above. When the courier
        receives this copy command it extracts the <code>ReadCap</code> from the given
          <code>WriteCap</code> and uses it to read the stream of data. The courier then reads a box
        at a time and tries to extract 0 or 1 envelopes from each accumulation of stream segments. </para>
      <para>After processing the command, the courier then overwrites the temporary stream with
        tombstones. </para>
    </section>
    <section xml:id="temp_stream" xmlns="http://docbook.org/ns/docbook"
      xmlns:xlink="http://www.w3.org/1999/xlink">
      <title xml:id="temp_stream.title">Temporary stream data format</title>
      <para> Each box in the temporary stream is a serialized <code>CopyStreamElement</code>.
        Defined in trunnel as: </para>
      <programlisting>// CopyStreamElement - wraps a CourierEnvelope chunk with stream position flags.
// Overhead: 1 byte (flags) + 4 bytes (envelope_len) = 5 bytes
struct copy_stream_element {
    // Flags: bit 0 = isStart, bit 1 = isFinal
    u8 flags;
  
    // The CourierEnvelope serialized bytes
    u32 envelope_len;
    u8 envelope_data[envelope_len];
}</programlisting>
      <para>Here it is in golang: </para>
      <programlisting>type CopyStreamElement struct {
    Flags        uint8
    EnvelopeLen  uint32
    EnvelopeData []uint8
}</programlisting>
      <para>The purpose of this specific format is to use the <code>isStart</code> and
          <code>isFinal</code> flags to tell the courier the first box and last box of the stream to
        process. The payloads encapsulated within the <code>EnvelopeData</code> fields of many of
        these <code>CopyStreamElement</code>s is itself a stream of data which contains
          <code>CourierEnvelope</code>s with four-byte prefixes. </para>
      <para>A key property of this encoding is that envelope boundaries do not align with box
        boundaries. Each BACAP box payload has a maximum size of N bytes, but a serialized
          <code>CourierEnvelope</code> (which contains a full box payload plus metadata) exceeds N
        bytes. Therefore envelopes are serialized into a continuous byte stream and split across
        multiple boxes in the temporary copy stream: </para>
      <programlisting>TEMPORARY COPY STREAM BOXES (each holds N bytes):
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│    Box 0    │ │    Box 1    │ │    Box 2    │ │    Box 3    │ │    Box 4    │
└─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘
SERIALIZED ENVELOPE DATA (envelope boundaries don't align with box boundaries):
┌─────────────────────────┐┌────────────────────┐┌──────────────────────────┐
│       Envelope 1        ││     Envelope 2     ││       Envelope 3         │
└─────────────────────────┘└────────────────────┘└──────────────────────────┘
|         |         |         |         |         |
     Box 0     Box 1     Box 2     Box 3     Box 4</programlisting>
      <para>The courier reads the stream box by box, accumulating data until it can extract complete
        envelopes. The <code>isStart</code> and <code>isFinal</code> flags on the
          <code>CopyStreamElement</code> wrappers tell the courier where the stream begins and ends. </para>
      <para>Each embedded <code>CourierEnvelope</code> is processed as a normal write and results in
        a <code>ReplicaMessage</code> being dispatched to the intermediate replicas over the wire
        protocol. </para>
      <para>The courier does NOT need to keep track of the <code>EnvelopeHash</code> for each
        contained <code>CourierEnvelope</code> for the purpose of replying to the client (the
        &quot;client&quot; of these envelopes being, in this case, the courier itself), but it does
        need to keep resending them until the intermediate replicas have ACKed them. </para>
      <para>The courier MUST keep track of the hash of the <code>CopyCommand</code> (computed as
          <code>BLAKE2b-256(write_cap)</code>) and MUST NOT process a given command more than once.
        This dedup cache has a TTL of 30 minutes (<code>CopyDedupCacheTTL</code> in <link
          xlink:href="https://github.com/katzenpost/katzenpost/blob/main/courier/server/plugin.go"
            ><code>courier/server/plugin.go</code></link>). </para>
    </section>
    <section>
      <title>CopyCommand</title>
      <para><code>CopyCommand</code> is sent by a client to its chosen courier after the client has
        successfully uploaded every box of the temporary stream. The trunnel layout is: </para>
      <programlisting>struct copy_command {
    u32 write_cap_len;
    u8  write_cap[write_cap_len];   // serialised BACAP BoxOwnerCap
}</programlisting>
      <para>On receipt, the courier: </para>
      <orderedlist numeration="arabic" spacing="compact">
        <listitem>
          <para>Computes <code>copyKey = BLAKE2b-256(write_cap)</code>. </para>
        </listitem>
        <listitem>
          <para>Consults its copy dedup cache. If an in-progress entry is found, the courier
            responds immediately with <code>CopyStatusInProgress</code>. If a completed entry within
            its TTL is found, the courier returns the cached terminal reply. </para>
        </listitem>
        <listitem>
          <para>Otherwise, the courier reconstructs the BACAP <code>WriteCap</code> from the bytes,
            derives the corresponding <code>ReadCap</code>, and reads the temporary stream box by
            box, feeding the decrypted BACAP payloads into a <code>CopyStreamEnvelopeDecoder</code>.
          </para>
        </listitem>
        <listitem>
          <para>Each complete <code>CourierEnvelope</code> emitted by the decoder is dispatched to
            its two intermediate replicas; the courier waits for acknowledgements with bounded
            retries and exponential backoff. </para>
        </listitem>
        <listitem>
          <para>Upon processing the box bearing the <code>isFinal</code> flag (or upon an
            unrecoverable failure), the courier attempts — on a best-effort basis — to overwrite
            every box of the temporary stream with a tombstone. </para>
        </listitem>
      </orderedlist>
      <para>Replica error handling during a <code>CopyCommand</code> is classified as <emphasis
          role="strong">temporary</emphasis> or <emphasis role="strong">permanent</emphasis> by
          <link
          xlink:href="https://github.com/katzenpost/katzenpost/blob/main/courier/server/copy_errors.go"
            ><code>courier/server/copy_errors.go</code></link>: transient errors (storage full,
        database failure, internal error, replication failed, box-ID not found) trigger bounded
        retries against the same shard before failover; permanent errors (invalid box ID, invalid
        signature, invalid payload, invalid epoch, box already exists, tombstone) cause immediate
        failover or abort. </para>
    </section>
    <section xml:id="copycommandreply" xmlns="http://docbook.org/ns/docbook"
      xmlns:xlink="http://www.w3.org/1999/xlink">
      <title xml:id="copycommandreply.title">CopyCommandReply</title>
      <para>The courier's reply to a <code>CopyCommand</code> has the following trunnel layout: </para>
      <programlisting>struct copy_command_reply {
    u8  status;                   // CopyStatus{Succeeded, InProgress, Failed}
    u8  error_code;               // replica error code (meaningful iff status == Failed)
    u64 failed_envelope_index;    // 1-based sequential position in the
                                  //   CourierEnvelope stream at which processing
                                  //   stopped (meaningful iff status == Failed)
}  </programlisting>
      <para>The status codes are enumerated in &quot;Copy command status codes&quot; above.
          <code>failed_envelope_index</code> counts <emphasis>envelopes</emphasis> within the
        stream, not boxes: the first envelope in the first box of the temporary stream is index 1. </para>
      <para>A client receiving <code>CopyStatusInProgress</code> should continue polling — i.e.
        resend the same <code>CopyCommand</code> via a fresh SURB after a short interval
          (<code>CopyPollInterval</code>, currently 5 seconds) — until it receives either
          <code>CopyStatusSucceeded</code> or <code>CopyStatusFailed</code>. Because the courier's
        copy dedup cache keys on <code>BLAKE2b-256(write_cap)</code>, these repeated polls will not
        cause the <code>CopyCommand</code> to be processed more than once. </para>
    </section>
    <section>
      <title>Potential use cases of AllOrNothing</title>
      <para>In no particular order: </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>Atomically writing to two or more boxes. </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>The boxes can reside on distinct streams (or not); the courier doesn't know
                anything about streams of <code>CourierEnvelope</code>. </para>
            </listitem>
          </itemizedlist>
        </listitem>
        <listitem>
          <para>Sending long messages that span more than one BACAP payload, like a file / document
            / picture. </para>
        </listitem>
        <listitem>
          <para>Group chat join uses AllOrNothing when adding a new member to the group: </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>The person introducing a new member writes to their group chat stream. </para>
            </listitem>
            <listitem>
              <para>The person introducing a new member also writes to the existing conversation
                stream that the new member can already read. </para>
            </listitem>
          </itemizedlist>
        </listitem>
        <listitem>
          <para>Group chat uses AllOrNothing in all cases where it needs to send long messages --
            files, pictures, audio, long cryptographic keys such as the group membership list, and
            so on. </para>
        </listitem>
      </itemizedlist>
    </section>
  </section>


  <section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Protocol narration example</title>
    <para><emphasis role="bold">Alice wants to send a message to Bob, who is already connected to
        Alice. That is, Alice will write to a box with ID 12345 that Bob is trying to read
        from.</emphasis>
    </para>    
    <orderedlist numeration="arabic">     
      <listitem>
        <para>Alice sends:</para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>SPHINX packet containing:</para>
            <itemizedlist>
              <listitem>
                <para>SPHINX header</para>
                <itemizedlist>
                  <listitem>
                    <para>Routing commands to make it arrive at a courier (on a service provider)</para>
                  </listitem>                  
                </itemizedlist>
              </listitem>
              <listitem>
                <para>SPHINX payload</para>
                <itemizedlist>
                  <listitem>
                    <para>Reply SURB</para>
                  </listitem>
                  <listitem>
                    <para><code>CourierEnvelope</code> encrypted for a courier chosen at random. The
                      envelope tells the courier about two randomly chosen replicas (&quot;intermediate replicas&quot;).</para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </listitem>
          <listitem>
            <para>Alice doesn't know if the packet makes it through the network. Until she
              receives a reply, she keeps resending the <code>CourierEnvelope</code> to the same courier.</para>
          </listitem>
        </orderedlist>
      </listitem>
      <listitem>
        <para>The courier receives the <code>CourierEnvelope</code> from Alice.</para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>The courier records the <code>EnvelopeHash</code> of
              the <code>CourierEnvelope</code> in its
              <code>CourierBookKeeping</code> datastructure.</para>
          </listitem>
          <listitem>
            <para>If the courier has already seen that hash, GOTO step 4.</para>
          </listitem>
        </orderedlist>
      </listitem>
      <listitem>
        <para>The courier sends an ACK to Alice using the SURB on file for Alice, telling her to
          stop resending the <code>CourierEnvelope</code>. If there's no SURB, the Courier waits for
          the next re-sent <code>CourierEnvelope</code> from Alice matching the
            <code>EnvelopeHash</code>.</para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>The courier constructs two <code>ReplicaMessage</code> objects and puts them in
              its outgoing queue for the two intermediate replicas. (Note that each courier
              maintains constant-rate traffic with all replicas).</para>
          </listitem>
          <listitem>
            <para>The courier keeps doing this for each intermediate replica until it receives an
              ACK from that replica.</para>            
          </listitem>
        </orderedlist>
      </listitem>
    </orderedlist>
    
    <para><emphasis role="bold">Alice’s actions are now complete.</emphasis></para>
    
    <orderedlist  continuation="continues">   
      <listitem>
        <para>The two intermediate replicas receive the two
          <code>ReplicaMessage</code> objects.</para>        
       <orderedlist numeration="upperalpha">
         <listitem>
           <para>They decrypt them and compute the &quot;designated
             replicas&quot; based on the BACAP box ID.</para>
         </listitem>
         <listitem>
           <para>The replicas put ACKs in their outgoing queues for the courier saying &quot;we have
              received these messages" (see step 3.2), referencing the <code>EnvelopeHash</code>.
              This tells the courier to stop resending to the intermediate replicas.</para>
         </listitem>      
         <listitem>
           <para>They put the decrypted contents (a
             <code>ReplicaWrite</code> BACAP tuple: box ID 12345;
             signature; encrypted payload) in their outgoing queues for the
             &quot;designated replicas&quot;.</para>
         </listitem>
         <listitem>
           <para>Each replica waits for a reply from its designated replica.</para>
         </listitem> 
         <listitem>
           <para>When an intermediate replica receives its ACK from the designated replica,
             the intermediate replica has no more tasks.</para>
         </listitem> 
       </orderedlist> 
      </listitem>
    </orderedlist>
    
    <para><emphasis role="bold">Bob now wants to check if Alice has written a message at box
        12345.</emphasis></para>
    
    <orderedlist  continuation="continues">
      <listitem>
        <para>Similar to step 1, Bob sends a SPHINX packet to a courier chosen at random.</para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>SPHINX packet containing:</para>
            <itemizedlist>
              <listitem>
                <para>SPHINX header</para>
                <itemizedlist>
                  <listitem>
                    <para>Routing commands to make it arrive at a courier (on a
                      service provider).</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para>SPHINX payload</para>
                <itemizedlist>
                  <listitem>
                    <para>The envelope tells the courier about two randomly chosen replicas
                      (“intermediate replicas”).</para>
                  </listitem>
                  <listitem>
                    <para><code>CourierEnvelope</code> encrypted for a
                      courier chosen at random. Its encrypted payload is a
                      <code>ReplicaRead</code> command (reference box ID
                      12345).</para>
                  </listitem>                 
                </itemizedlist>
              </listitem>         
            </itemizedlist>
          </listitem>
          <listitem>
            <para>Bob doesn't know if the packet makes it through the network. Until he
              receives a reply, he keeps resending the <code>CourierEnvelope</code> to the same courier.</para>
          </listitem>        
        </orderedlist>
      </listitem>

      <listitem>
        <para>The courier receives Bob's <code>CourierEnvelope</code>. (Similar to step 2). </para>
      </listitem>
      <listitem>
        <para> The courier sends an ACK to Bob. (Similar to step 3.) </para>
      </listitem>
      <listitem>
        <para> The two intermediate replicas receive the two <code>ReplicaMessage</code> objects
          containing Bob's <code>ReplicaRead</code>.</para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>Similar to 4.A.</para>
          </listitem>
          <listitem>
            <para>Similar to 4.B.</para>
          </listitem>
          <listitem>
            <para>Similar to 4.C.</para>
          </listitem>
          <listitem>
            <para>Similar to 4.D.</para>
          </listitem>
          <listitem>
            <para>When the intermediate replica receives its ACK from the designated replica, it
              will include the (perhaps confusingly named) <code>ReplicaWrite</code> (BACAP
              tuple).</para>
          </listitem>
          <listitem>
            <para>The intermediate replica wraps it in a <code>ReplicaMessageReply</code> encrypted
              for Bob's <code>ReplicaMessage.EPubKey</code> and puts it in its outgoing queue for
              the Courier.</para>
          </listitem>
        </orderedlist>
      </listitem>
      <listitem>
        <para>The Courier receives two <code>ReplicaMessageReply</code> wire commands from the two
          intermediate replicas. </para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>It matches <code>ReplicaMessageReply.EnvelopeHash</code> to its recorded
          bookkeeping state (step 2.1). </para>
          </listitem>
          <listitem>
            <para>It wraps the replica's <code>EnvelopeReply</code>
              blob (the MKEM-encrypted
              <code>ReplicaMessageReplyInnerMessage</code>) into a
              <code>CourierEnvelopeReply</code> with
              <code>reply_type = PAYLOAD</code>, and — if Bob's latest
              retransmission arrived with a fresh Sphinx SURB — forwards the
              resulting <code>CourierQueryReply</code> back through the
              mixnet to Bob.</para>
          </listitem>
          <listitem>
            <para>Either way the courier retains the
              <code>ReplicaMessageReply</code> in its dedup cache for
              the cache TTL (5 minutes), so that a subsequently-arriving
              retransmission of the same envelope can be served from cache
              without re-dispatching to replicas.</para>
          </listitem>
        </orderedlist>
      </listitem>
      <listitem>
        <para>Bob keeps resending his <code>CourierEnvelope</code> from step 5 until he receives a
          <code>CourierEnvelopeReply</code> with <code>reply_type = PAYLOAD</code>. </para>
        <orderedlist numeration="upperalpha">
          <listitem>
            <para>Bob decapsulates the MKEM <code>EnvelopeReply</code> with the private key
              corresponding to the <code>sender_pubkey</code> he put in his
              <code>CourierEnvelope</code>, obtaining the padded
              <code>ReplicaMessageReplyInnerMessage</code>.</para>
          </listitem>
          <listitem>
            <para>Once unpadded, the inner message is either a <code>ReplicaReadReply</code>
          carrying the BACAP tuple (box ID, signature, ciphertext) or a non-success
            <code>error_code</code>. If the code is <code>ReplicaErrorBoxIDNotFound</code>, Bob
          waits and polls again (i.e. returns to step 5). If it is
            <code>ReplicaErrorTombstone</code>, Alice has deleted the message. Otherwise Bob
          BACAP-decrypts and verifies the payload. </para>
          </listitem>
        </orderedlist>
      </listitem>
    </orderedlist>
   
    <para><emphasis role="bold">Bob can now read Alice's message.</emphasis></para>
    
  </section>
</section>
